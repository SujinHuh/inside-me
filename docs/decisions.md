# Inside Me 결정 로그

이 문서는 제품과 기술의 결정뿐 아니라 아직 제안 또는 보류 상태인 선택지를 함께 관리한다. 결정의 결론만 남기지 않고 왜 선택했는지, 무엇을 포기했는지, 언제 다시 검토할지를 보존한다.

## 상태 정의

- `제안됨`: 선택지와 권고안이 나왔지만 사용자가 확정하지 않음
- `임시 운영`: 최종 형식은 미확정이지만 작업 연속성을 위해 되돌릴 수 있는 기본안으로 사용 중
- `확정`: 현재 적용할 결정으로 사용자가 선택했거나 명확한 요구로 지정함
- `보류`: 지금 결정하지 않기로 함
- `거절`: 검토 후 선택하지 않음
- `대체됨`: 이후 결정이 이 결정을 대신함

## DEC-001 — 프로젝트 작업명은 Inside Me

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-001
- 관련 요구사항: 없음

### 맥락

개인 감정일기 앱을 시작하기 위한 폴더와 작업명이 필요했다.

### 선택지

`mood-pudding`, `dear-mood`, `mood-pocket`, `heart-log`, `my-mood-diary`, `inside-me`, `maeum-note`, `one-day-mood`.

### 결정과 이유

사용자가 `inside-me`를 선택했다. 내면의 감정을 들여다보는 제품 방향과 잘 맞고 사용자가 선호한다.

### 감수하는 비용과 위험

이름만으로 감정일기 앱인지 즉시 알기 어렵다.

### 재검토 조건

외부 공개나 앱스토어 출시를 준비하며 상표·검색성·도메인을 검토할 때.

## DEC-002 — 첫 사용자는 솔로 개발자 본인

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-001, LOG-20260818-002
- 관련 요구사항: REQ-010

### 결정과 이유

사용자가 먼저 매일 도그푸딩하며 핵심 흐름의 가치를 확인한다. 다중 사용자와 운영 기능보다 개인 사용의 완결성을 우선한다.

### 감수하는 비용과 위험

한 사람에게 최적화되어 다른 사용자의 언어, 접근성, 감정 표현 방식을 놓칠 수 있다.

### 재검토 조건

본인의 반복 사용성이 확인되어 다른 사용자에게 테스트를 요청할 때.

## DEC-003 — AI의 감정 후보 제시 방식

- 상태: 대체됨 → DEC-037
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-002, LOG-20260818-003, LOG-20260818-009, LOG-20260820-002
- 관련 요구사항: REQ-001, REQ-009, REQ-026

### 맥락

사용자는 AI가 “진정한 감정”을 찾도록 돕기를 원한다. 그러나 감정은 AI가 객관적 정답처럼 확정할 수 없다.

정신건강 상태를 진단하지 않고 감정·타인의 의도를 단정하지 않는 `REQ-009` 안전 원칙은 이 결정과 별개로 적용 중이다. 여기서 미확정인 부분은 AI가 감정 후보를 몇 개, 어떤 방식으로 제시할지다.

### 선택지

1. AI가 하나의 감정을 결론으로 제시한다.
2. AI가 근거와 함께 후보를 제시하고 사용자가 선택한다.
3. AI는 질문만 하고 후보를 제시하지 않는다.

### 권고안과 이유

2번. 구체적인 도움을 제공하면서도 사용자의 자기결정권과 오류 수정 가능성을 지킨다.

### 감수하는 비용과 위험

사용자가 즉각적인 명확한 답을 기대할 때 답답하게 느낄 수 있다. 후보의 표현과 수가 적절하지 않으면 선택 피로가 생길 수 있다.

### 재검토 조건

사용자가 이 원칙을 직접 확정하거나 도그푸딩에서 후보 제시가 방해된다는 반복 신호가 나올 때.

## DEC-004 — 취침 전 하루 한 번의 기본 체크인

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-003
- 관련 요구사항: REQ-006

### 결정과 이유

밤 10시나 11시 등 사용자가 정한 취침 전 시간에 하루를 돌아보는 알림을 제공한다.

### 감수하는 비용과 위험

늦은 밤의 긴 감정 탐색이 수면을 방해할 수 있고, 알림이 의무감이나 피로를 줄 수 있다.

### 재검토 조건

도그푸딩에서 반복적으로 알림을 미루거나 체크인 후 각성이 높아질 때.

## DEC-005 — 음성과 글 입력을 모두 지원

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-003
- 관련 요구사항: REQ-002

### 결정과 이유

사용자가 상황과 에너지에 따라 말하거나 타이핑할 수 있게 한다.

### 감수하는 비용과 위험

음성 권한, 전사 오류, 비용, 민감정보 전송과 원본 음성 보존 정책이 추가된다.

### 재검토 조건

없음. 구현 순서와 전사 방식은 별도 결정한다.

## DEC-006 — 월간 달력에 얼굴 표정으로 날짜별 기록 표시

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-003
- 관련 요구사항: REQ-007, REQ-008

### 결정과 이유

월간 달력에서 하루의 상태를 빠르게 보고, 날짜를 선택해 당시의 이야기와 감정을 다시 확인한다.

### 감수하는 비용과 위험

복합적인 하루를 하나의 표정으로 축약할 수 있고, 색상과 얼굴이 특정 감정을 좋음·나쁨으로 도덕화할 수 있다.

### 재검토 조건

아이콘 체계를 설계하거나 하루 복수 기록 정책을 결정할 때.

## DEC-007 — 첫 음성 UX는 턴 방식

- 상태: 확정
- 일자: 2026-08-18
- 상태 변경 이력: LOG-20260818-003에서 제안, LOG-20260818-005에서 사용자 확정
- 관련 로그: LOG-20260818-003, LOG-20260818-005
- 관련 요구사항: REQ-002, REQ-003

### 선택지

1. 실시간 통화형 음성 대화
2. 자유 발화 → 전사 확인 → 질문 하나 → 음성/글 답변의 턴 방식
3. 최초 발화만 음성이고 이후는 글

### 결정과 이유

2번을 첫 MVP에 사용한다. 사용자는 글보다 말하면서 생각을 정리하는 방식을 선호하고, 여러 턴에 걸쳐 AI와 대화하며 감정을 차츰 구체화하기를 원한다. 전사를 확인할 수 있어 오전사를 고칠 수 있고 솔로 개발 MVP의 구현 복잡도와 비용도 낮춘다.

### 감수하는 비용과 위험

실시간 통화 같은 몰입감과 즉각성은 낮아진다.

### 재검토 조건

턴 방식 프로토타입에서 흐름이 반복적으로 끊긴다고 느낄 때.

## DEC-008 — 요구사항·원문·결정의 3계층 추적 체계

- 상태: 임시 운영 (정확한 문서 형식은 사용자 미확정)
- 일자: 2026-08-18
- 상태 변경 이력: LOG-20260818-004에서 제안, LOG-20260818-009에서 임시 운영 상태를 명확히 함
- 관련 로그: LOG-20260818-004, LOG-20260818-009
- 관련 요구사항: REQ-011

### 맥락

현재 요구사항 문서만으로는 아이디어의 원문, 결정 과정과 트레이드오프가 사라질 수 있다.

### 선택지

1. `requirements.md` 하나에 모든 내용을 누적한다.
2. 현재 요구사항, 시간순 제품 로그, 결정 로그를 분리한다.
3. 모든 대화 전문만 저장하고 별도 구조화 문서는 만들지 않는다.

### 권고안과 이유

2번을 기본 운영 방식으로 적용한다.

- `requirements.md`: 현재 유효한 요구
- `product-log.md`: 사용자 원문과 당시 해석·제안
- `decisions.md`: 선택지, 상태, 이유, 트레이드오프와 재검토 조건

각 문서는 `REQ-NNN`, `LOG-YYYYMMDD-NNN`, `DEC-NNN`으로 연결한다.

### 감수하는 비용과 위험

매 대화에서 세 문서의 일관성을 관리해야 한다. 로그가 길어질 수 있다.

### 재검토 조건

로그 탐색이 어려워지거나 중복 관리 비용이 실제 개발 흐름을 방해할 때 월별 분할 또는 자동화를 검토한다.

## DEC-009 — 음성 우선, 글 입력도 함께 지원

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-005
- 관련 요구사항: REQ-002, REQ-003, REQ-012

### 맥락

사용자는 글을 쓰는 것보다 말하면서 생각을 정리하고 감정을 구체화하는 경험을 더 선호하지만 두 입력 방식을 모두 원한다.

### 결정과 이유

음성을 시작 화면의 기본 행동으로 강조하되 글 입력도 첫 버전부터 함께 제공한다. 사용자는 글보다 말하면서 생각을 정리하는 방식을 선호하지만 상황에 따라 글을 사용할 수 있기를 원한다.

### 감수하는 비용과 위험

- 두 입력 방식을 첫 MVP부터 지원하므로 음성만 구현할 때보다 입력 상태와 오류 복구가 복잡하다.
- 음성 우선 UI가 조용한 공간이나 말하기 어려운 상황의 사용자를 소외시키지 않도록 글 전환을 쉽게 유지해야 한다.

### 재검토 조건

도그푸딩에서 글 모드가 거의 사용되지 않거나, 한 흐름에서 입력 방식을 전환할 때 반복적인 오류가 생길 때.

## DEC-010 — 음성과 글이 하나의 탐색 로직을 공유

- 상태: 대체됨 → DEC-011
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-005
- 관련 요구사항: REQ-002, REQ-003, REQ-012

### 맥락

사용자가 말한 “두 가지 버전”이 두 개의 독립된 화면 흐름인지, 같은 흐름의 두 입력 방식인지는 명시적으로 확정되지 않았다.

### 선택지

1. 음성과 글을 완전히 별개의 기능과 화면 흐름으로 개발한다.
2. 하나의 감정 탐색 흐름에서 음성과 글이 같은 질문 로직, 감정 후보, 결과와 저장 형식을 공유한다.

### 권고안과 이유

2번. 감정 탐색의 본질은 같고 입력 인터페이스만 다르므로 중복 개발과 두 모드 간 동작 차이를 줄인다. 어느 턴에서든 입력 방식을 전환할 수 있게 한다.

### 감수하는 비용과 위험

- 음성에 맞춘 짧은 AI 응답과 글에서 읽기 좋은 응답 길이가 달라 표현 방식을 조정해야 할 수 있다.
- 사용자가 기대한 “두 가지 버전”이 서로 다른 경험이었다면 단일 흐름이 기대를 충족하지 못할 수 있다.

### 재검토 조건

사용자가 “두 가지 버전”의 의미를 확인하거나 프로토타입에서 모드별로 다른 흐름이 필요하다고 느낄 때.

## DEC-011 — 한 앱 안에 세 가지 감정 기록 경험

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-006
- 관련 요구사항: REQ-002, REQ-003, REQ-012
- 대체한 결정: DEC-010

### 결정과 이유

한 앱 안에 다음 세 가지 경험을 둔다.

1. 먼저 자유롭게 길게 말하는 음성 기록
2. 먼저 자유롭게 작성하는 글 기록
3. AI와 음성으로 한 턴씩 주고받으며 감정을 구체화하는 대화

사용자는 말하기, 쓰기, 대화하기 중 그날 편한 방식을 선택할 수 있다.

### 감수하는 비용과 위험

- 세 진입 경험의 차이가 UI에서 명확하지 않으면 사용자가 무엇을 골라야 할지 혼란스러울 수 있다.
- 자유 음성과 글 기록이 이후 AI 질문으로 이어질 때 턴 대화와 경계가 흐려질 수 있다.
- 세 경험의 공통 로직과 개별 로직을 적절히 나누지 않으면 솔로 개발 범위가 커질 수 있다.

### 재검토 조건

각 경험의 첫 프로토타입을 사용했을 때 둘 이상의 경험이 사실상 동일하게 느껴지거나 거의 사용되지 않을 때.

## DEC-012 — 안드로이드폰 우선, 컴퓨터 보조

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-006
- 관련 요구사항: REQ-013

### 결정과 이유

첫 도그푸딩은 안드로이드폰에서 진행하고 컴퓨터에서도 사용할 수 있게 한다. 사용자는 아이폰을 사용하지 않으므로 iOS는 초기 범위에서 제외한다.

### 감수하는 비용과 위험

안드로이드와 컴퓨터의 마이크, 알림, 로컬 저장 동작이 달라 두 환경을 검증해야 한다. iOS 사용성은 검증되지 않는다.

### 재검토 조건

기술 스택 결정 시 두 환경을 한 코드베이스로 지원하기 어렵거나 외부 사용자 테스트에서 iOS 필요성이 생길 때.

## DEC-013 — AI의 공감과 질문을 음성으로 제공

- 상태: 확정, 기본 재생 방식은 프로토타입 후 결정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-006
- 관련 요구사항: REQ-014

### 결정과 이유

사용자가 말로 감정을 탐색할 때 AI의 공감 표현과 다음 질문도 목소리로 들을 수 있게 한다. 자동 재생, 선택 재생, 음성 길이와 속도는 도그푸딩 결과를 바탕으로 AI가 권고하고 사용자가 확정한다.

### 감수하는 비용과 위험

음성 합성 비용과 지연이 생기며 늦은 밤이나 공공장소에서는 자동 재생이 불편할 수 있다. 화면의 글도 함께 제공하고 쉽게 음소거할 수 있어야 한다.

### 재검토 조건

프로토타입에서 음성 응답이 흐름을 자주 끊거나 글만 읽는 비율이 높을 때.

## DEC-014 — MVP 기록은 현재 기기에만 저장

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-006
- 관련 요구사항: REQ-015

### 결정과 이유

첫 MVP에서는 감정 기록을 현재 기기에만 저장하고 계정과 기기 간 동기화 서버는 만들지 않는다. 동기화는 실제 필요가 확인될 때 추가한다.

### 감수하는 비용과 위험

안드로이드와 컴퓨터의 기록이 자동으로 공유되지 않는다. 기기 분실이나 앱 데이터 삭제 시 기록을 잃을 수 있으므로 내보내기와 백업 방법이 필요하다.

클라우드 AI와 음성 처리 API를 사용한다면 기록 동기화와 별개로 API 키 보호를 위한 작은 중계 백엔드가 필요할 수 있다.

### 재검토 조건

여러 기기에서 같은 기록을 보고 싶은 필요가 반복되거나 데이터 손실 위험이 도그푸딩을 방해할 때.

## DEC-015 — 한 번 탭하여 녹음 시작, 다시 탭하여 종료

- 상태: 확정
- 일자: 2026-08-18
- 상태 변경 이력: LOG-20260818-006에서 제안, LOG-20260818-007에서 사용자 확정
- 관련 로그: LOG-20260818-006, LOG-20260818-007
- 관련 질문: Q-003
- 관련 요구사항: REQ-002, REQ-012

### 선택지

1. 버튼을 누르고 있는 동안만 녹음한다.
2. 버튼을 한 번 눌러 시작하고 다시 눌러 종료한다.
3. 사용자의 침묵을 감지해 자동으로 종료한다.

### 결정과 이유

2번. 사용자가 길게 이야기할 때 버튼을 계속 누를 필요가 없고, 감정을 생각하느라 생기는 침묵을 앱이 발화 종료로 잘못 판단하지 않는다. 사용자가 이 권고안을 수용했다.

### 감수하는 비용과 위험

사용자가 종료 버튼을 누르는 것을 잊을 수 있다. 녹음 중임을 명확하게 표시하고 경과 시간과 최대 녹음 시간을 제공해야 한다.

### 재검토 조건

도그푸딩에서 종료 버튼 누르기가 대화 흐름을 반복적으로 방해할 때.

## DEC-016 — 원본 음성은 처리 후 삭제

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-007
- 관련 요구사항: REQ-016

### 결정과 이유

사용자는 자신의 목소리를 다시 들을 필요가 없다. 원본 음성은 텍스트 전사와 분석에 필요한 동안만 임시로 사용하고 처리가 성공하면 삭제한다. 사용자가 확인한 전사 텍스트와 감정 탐색 결과는 기록으로 남긴다.

### 감수하는 비용과 위험

- 전사 오류를 나중에 원본 음성과 대조할 수 없다.
- 처리 직후 삭제에 실패하는 경우를 대비한 재시도와 임시 파일 정리 로직이 필요하다.
- 외부 음성 처리 서비스가 별도로 데이터를 보존하지 않는지 제공자 정책을 확인해야 한다.

### 재검토 조건

전사 품질 문제로 원본을 잠시 보관할 필요가 반복적으로 생길 때 사용자의 명시적 선택 아래 단기 보관을 다시 검토한다.

## DEC-017 — 기본 탐색은 약 5분, 질문 3~6개

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-007
- 관련 요구사항: REQ-017

### 결정과 이유

취침 전 부담을 낮추기 위해 기본 감정 탐색은 약 5분 동안 핵심 질문 3~6개를 목표로 한다. 사용자가 더 이야기하고 싶으면 이어갈 수 있고 충분하다고 느끼면 일찍 끝낼 수 있다.

### 감수하는 비용과 위험

고정된 질문 수를 목표처럼 보이게 하면 충분히 탐색되지 않았는데 서두를 수 있다. 시간과 질문 수는 진행 안내에만 사용하고 강제 종료 조건으로 사용하지 않는다.

### 재검토 조건

도그푸딩에서 반복적으로 너무 길거나 얕다고 느낄 때.

## DEC-018 — MVP는 하루 기록 하나

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-007
- 관련 요구사항: REQ-018

### 결정과 이유

첫 버전에서는 하루에 있었던 사건과 감정을 하나의 일일 기록으로 묶어 정리한다. 사건별 복수 기록은 제품이 확장될 때 추가한다.

### 감수하는 비용과 위험

- 서로 다른 사건과 감정이 하나의 기록 안에서 섞일 수 있다.
- 낮에 기록을 시작하고 밤에 다시 쓰려면 새 기록 생성이 아니라 당일 기록 이어쓰기와 병합이 필요하다.
- 달력 표시는 단순하지만 하루의 여러 감정을 대표 표정 하나로 정리하는 규칙이 필요하다.

### 재검토 조건

하루에 감정적으로 중요한 사건이 여러 번 생겨 하나의 기록으로 정리하기 불편한 날이 반복될 때.

## DEC-019 — AGENTS.md는 실행 지침, 상세 제품 정보는 docs가 원본

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-008, LOG-20260818-009
- 관련 요구사항: REQ-011
- 공식 근거: [OpenAI의 AGENTS.md 공식 문서](https://developers.openai.com/codex/guides/agents-md)

### 결정과 이유

프로젝트의 `AGENTS.md`에는 Codex가 매 작업에서 따라야 하는 저장소 범위, 문서 원본, 작업 순서, 로깅, 안전·개인정보, 의존성·비밀정보, 검증, 코드 리뷰와 완료 기준을 둔다.

상세 요구사항, 사용자 원문, 결정 이력과 미결정 질문은 각각 `requirements.md`, `product-log.md`, `decisions.md`, `open-questions.md`를 원본으로 유지한다. `AGENTS.md`가 이 내용을 전부 복제하지 않고 작업 전에 해당 문서를 읽도록 지시한다.

### 공식 문서에서 반영한 기준

- Codex는 작업 전에 `AGENTS.md` 지침을 읽는다.
- 저장소 루트에서 현재 작업 디렉터리까지 지침을 합치며 더 가까운 디렉터리의 지침이 더 구체적으로 적용된다.
- 저장소 설정, 테스트·검증 명령, 의존성 추가 규칙과 문서 갱신처럼 실제 작업 행동을 명시한다.
- 하위 영역에 다른 규칙이 필요할 때만 가까운 위치에 별도 지침 또는 override를 둔다.
- 지침 크기 제한을 고려해 실행 규칙은 간결하게 유지하고 상세 자료는 연결한다.

### 감수하는 비용과 위험

- 지침과 상세 문서가 나뉘므로 에이전트가 작업 전 여러 문서를 읽어야 한다.
- 기술 스택이 미정이라 설치·테스트·빌드의 정확한 명령은 아직 기록할 수 없다.
- 프로젝트보다 위 디렉터리에서 세션을 시작하면 중첩된 `inside-me/AGENTS.md`가 발견되지 않을 수 있다.

### 재검토 조건

기술 스택과 저장소 스크립트가 확정될 때 실행 명령을 추가한다. 하위 앱이나 서비스가 독립 규칙을 필요로 할 때만 중첩 `AGENTS.md`를 검토한다.

## DEC-020 — 기능 경계별로 개발 단위를 분리

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-010
- 관련 요구사항: REQ-019

### 맥락

사용자는 메인 화면, 달력과 표정 표시, 음성 일기, 글 일기, AI 턴 대화를 서로 구분된 기능으로 개발하기를 원한다.

### 결정과 이유

다음 기능을 독립적으로 이해하고 테스트할 수 있는 모듈로 나눈다.

- 메인과 기록 시작
- 월간 달력과 날짜별 상세
- 감정·욕구 어휘와 선택
- 자유 음성 기록
- 자유 글 기록
- AI 턴 대화
- 알림
- 로컬 기록 저장
- 녹음, 전사, 음성 합성, AI 모델 같은 외부 기능 연동

여기서 “API별 분리”는 화면마다 서버를 따로 만든다는 뜻이 아니다. 화면은 사용자 흐름을 조합하고, 재사용할 핵심 로직과 외부 연동 경계를 기능 단위로 분리한다. 음성과 글은 입력 모듈은 다르지만 감정 탐색과 저장 규칙은 공유한다.

### 감수하는 비용과 위험

- 아주 작은 MVP에도 폴더, 인터페이스와 테스트 경계가 조금 늘어난다.
- 경계를 지나치게 잘게 나누면 기능 하나를 수정할 때 여러 파일을 오가게 된다.
- 반대로 화면에 모든 로직을 넣지 않으므로 이후 음성 공급자나 저장 방식을 바꾸기 쉽다.

### 재검토 조건

도그푸딩에서 두 모듈이 항상 함께 바뀌어 경계가 오히려 방해되거나, 새로운 기능이 현재 경계에 자연스럽게 들어가지 않을 때.

## DEC-021 — MVP 배포 구조는 모듈형 모놀리스

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-010, LOG-20260819-001
- 관련 요구사항: REQ-019, REQ-020

### 맥락

기능별 분리를 유지하면서 모놀리스와 마이크로서비스 중 어떤 배포 구조가 솔로 개발자의 일주일 MVP에 적합한지 판단해야 한다.

### 선택지

1. 기능마다 독립 서버와 배포 단위를 갖는 마이크로서비스
2. 하나의 앱과 필요할 때 하나의 얇은 API 중계만 두되 내부 코드는 기능 모듈로 나누는 모듈형 모놀리스
3. 경계 없이 화면과 외부 API 호출을 한곳에 빠르게 작성하는 단순 모놀리스

### 결정과 이유

사용자가 2번 방향을 수용했다. 한 사람이 일주일 안에 도그푸딩하려면 여러 서비스의 배포, 인증, 관측, 네트워크 실패와 데이터 일관성을 운영할 이유가 없다. 기능 모듈 경계는 유지하되 서버가 필요하면 하나의 얇은 중계로 둔다. 일기 기록은 기기에 남기고, 외부 AI 비밀 키를 보호하는 중계와 기록 동기화 서버를 구분한다.

모놀리스는 모든 코드를 한 파일이나 한 폴더에 섞는다는 뜻이 아니다. 하나의 서버 배포 단위 내부를 기능·계층별로 분리할 수 있다. 모바일 클라이언트와 서버 런타임은 서로 다른 실행 환경이므로 구분하되, 서버를 기능마다 독립 배포하는 MSA는 사용하지 않는다.

### 감수하는 비용과 위험

- 나중에 사용자가 크게 늘면 일부 기능을 독립 서비스로 떼어낼 작업이 생길 수 있다.
- 모듈 경계를 코드 리뷰와 테스트로 지키지 않으면 시간이 지나며 결합될 수 있다.
- 중계 API를 추가하면 일기 저장 서버가 없어도 네트워크와 최소 운영 비용은 생긴다.

### 재검토 조건

독립 확장이 필요한 부하, 서로 다른 보안 경계, 별도 배포 주기 또는 다수 개발팀이 실제로 생길 때.

## DEC-022 — TypeScript와 Expo 기반 범용 앱 권고

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-010, LOG-20260819-001, LOG-20260819-002
- 관련 요구사항: REQ-013, REQ-019, REQ-020
- 공식 근거: [Expo 첫 앱 공식 문서](https://docs.expo.dev/tutorial/create-your-first-app/), [Expo 웹 공식 문서](https://docs.expo.dev/workflow/web/), [Expo Audio](https://docs.expo.dev/versions/latest/sdk/audio/), [Expo Notifications](https://docs.expo.dev/versions/latest/sdk/notifications/), [Android의 Kotlin 공식 안내](https://developer.android.com/kotlin)

### 맥락

안드로이드폰을 우선 사용하면서 컴퓨터에서도 쓰고, 음성 녹음과 취침 전 알림을 포함한 MVP를 빠르게 만들어야 한다. Java와 TypeScript를 포함한 구현 선택지를 비교했다.

### 선택지

1. Java 기반 네이티브 Android 앱과 별도 컴퓨터용 웹 앱
2. Kotlin과 Jetpack Compose 기반 네이티브 Android 앱과 별도 컴퓨터용 웹 앱
3. TypeScript, React Native, Expo와 Expo Router로 Android와 웹을 함께 구현
4. TypeScript 웹 앱을 먼저 만들고 Capacitor로 Android를 포장

### 권고안과 이유

3번을 권고한다. Expo 기본 프로젝트는 TypeScript, Android, iOS와 웹 구성을 제공하고 React Native for Web을 통해 공통 UI와 로직을 재사용할 수 있다. `expo-audio`는 Android와 웹에서 녹음을 지원하고, `expo-notifications`는 Android에서 서버 없이 반복 로컬 알림을 예약할 수 있어 현재 요구와 맞는다.

Java는 안드로이드 전용이라 컴퓨터용 화면을 별도로 만들어야 한다. 네이티브 Android를 선택해야 한다면 현대 Android UI 툴킷이 Kotlin 기반이므로 Java보다 Kotlin을 우선 비교하는 편이 타당하다. Capacitor는 웹 CSS 중심의 화면에 유리하지만 이번 앱은 Android 음성·알림 경험이 핵심이므로 Expo를 첫 권고로 둔다.

앱 내부에는 `EntryRepository`, `Recorder`, `Transcriber`, `ConversationAI`, `SpeechPlayer`, `Reminder` 같은 경계를 두어 플랫폼과 공급자 구현을 교체할 수 있게 한다. 외부 AI 키가 필요하면 클라이언트에 넣지 않고 TypeScript 기반의 얇은 중계 API를 별도 패키지로 추가한다.

### 감수하는 비용과 위험

- Android와 웹의 마이크 권한, 오디오 포맷, 알림과 저장 구현은 완전히 같지 않아 플랫폼별 어댑터가 필요하다.
- `expo-notifications`는 Android와 iOS 대상이므로 컴퓨터 웹 알림은 별도 구현 또는 후순위가 된다.
- Expo SQLite의 웹 지원은 현재 알파이며 추가 브라우저 보안 헤더가 필요하다. 따라서 저장 인터페이스는 공유하되 Android와 웹 저장 구현을 분리하거나 웹 범위를 줄여야 한다.
- React Native 스타일링은 일반 웹 CSS와 차이가 있어 아주 정교한 데스크톱 전용 레이아웃은 플랫폼별 보완이 필요할 수 있다.

### 재검토 조건

네이티브 Android 기능이 Expo로 막히거나, 컴퓨터 웹 경험이 Android보다 더 중요한 것으로 바뀌거나, 프로토타입에서 React Native 스타일링이 선택한 시각 방향을 구현하기 어렵다고 확인될 때.

### 진행 상태

사용자는 2026-08-19에 TypeScript를 수용한 뒤 React Native, Expo와 Expo Router까지 앞서 제안한 기술 스택으로 진행하는 것이 맞다고 확인했다. 확정된 언어 선택은 DEC-025에도 분리되어 있다.

## DEC-023 — 부드러운 레트로와 현대적 사용성을 결합한 UI 권고

- 상태: 제안됨
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-010, LOG-20260818-011, LOG-20260819-001
- 관련 요구사항: 없음

### 맥락

사용자는 최근 AI 제품에서 반복되는 지나치게 매끈하고 비슷한 UI를 피하고, 1980~1990년대 또는 Windows XP를 떠올리는 앤틱한 분위기를 고려하고 있다. 아직 참고 화면을 더 찾아볼 예정이다.

### 선택지

1. Windows XP를 픽셀 단위로 재현하는 강한 복고 UI
2. 현재 AI·SaaS 제품에서 흔한 카드, 둥근 모서리와 그라데이션 중심의 현대 UI
3. 옛날 개인용 다이어리·데스크톱 도구의 촉감을 차용하되 가독성, 접근성, 모바일 조작은 현대적으로 유지하는 부드러운 레트로 UI

### 권고안과 이유

3번을 권고한다. 얇은 창 테두리, 제목 표시줄, 눌리는 버튼, 제한된 색상, 작은 표정 아이콘과 종이·CRT를 연상시키는 질감을 선택적으로 사용하고, 본문 글자 크기·대비·터치 영역과 정보 구조는 현대 기준을 유지한다. 감정을 다루는 앱의 차분함을 해치지 않으면서도 일반적인 AI 채팅 앱과 구별할 수 있다.

정확한 미술 방향은 사용자가 가져올 참고 화면을 바탕으로 2~3개의 작은 목업을 비교한 뒤 확정한다.

### 감수하는 비용과 위험

- 복고 장식이 많으면 감정에 집중하기 어렵고 장난감처럼 보일 수 있다.
- XP를 그대로 모사하면 작은 모바일 화면과 접근성에 맞지 않을 수 있다.
- 독특한 시각 언어를 만들려면 기본 컴포넌트 라이브러리를 그대로 쓰는 것보다 디자인 토큰과 세부 조정 시간이 더 든다.

### 재검토 조건

참고 화면 비교와 실제 안드로이드 목업에서 분위기가 부담스럽거나 감정 기록의 가독성을 해칠 때.

## DEC-024 — Hallmark를 UI 설계·검수 참고 기준으로 사용

- 상태: 확정
- 일자: 2026-08-18
- 관련 로그: LOG-20260818-011
- 관련 요구사항: REQ-021
- 외부 참고: [Hallmark 공식 저장소](https://github.com/Nutlope/hallmark), [Hallmark SKILL.md](https://github.com/Nutlope/hallmark/blob/main/skills/hallmark/SKILL.md)

### 맥락

사용자는 AI가 만든 화면에서 자주 보이는 특유의 촌스럽고 획일적인 느낌을 줄이기 위해 Hallmark를 참고해 달라고 요청했다.

### 결정과 이유

Hallmark의 원본 저장소와 디자인·audit·redesign·study 흐름, anti-pattern 목록과 slop test를 Inside Me UI 설계 및 리뷰의 참고 기준으로 사용한다. 특히 제품 고유성, 정보 위계, 절제, 구조적 다양성, 디자인 토큰, 상호작용 상태와 접근성 검수를 채택한다.

Hallmark를 디자인의 최종 결정권자로 사용하지 않는다. 우선순위는 다음과 같다.

1. 사용자가 직접 제시한 요구와 참고 화면
2. 감정 기록 제품의 차분함, 안전과 접근성
3. 확정된 Inside Me 디자인 시스템
4. Hallmark에서 현재 화면에 관련된 점검 원칙

Hallmark는 랜딩 페이지와 웹 UI까지 폭넓게 다루므로 hero, navigation, footer와 macrostructure 규칙을 모바일 앱 화면에 기계적으로 적용하지 않는다. 프로젝트에 맞게 조정한 기준은 `docs/references/hallmark.md`에 유지한다.

현재는 원본을 검토하고 프로젝트 참고 문서와 지침에 연결했다. 외부 Hallmark 스킬 자체는 아직 설치하지 않았다.

### 감수하는 비용과 위험

- 체크리스트가 길어지면 일주일 MVP의 구현 속도를 늦출 수 있다.
- “AI 같아 보이는 요소”를 기계적으로 금지하면 사용자에게 익숙하고 유용한 UI 패턴까지 배제할 수 있다.
- Hallmark의 일부 기준은 웹 랜딩 페이지 중심이어서 React Native 모바일 앱에 그대로 맞지 않는다.
- 외부 스킬이 바뀌면 프로젝트가 참고한 버전과 최신 원본 사이에 차이가 생길 수 있다.

### 재검토 조건

사용자가 참고 화면을 제공해 실제 Inside Me 디자인 시스템을 확정할 때, 또는 Hallmark 스킬 설치가 구현 품질과 속도에 실제로 도움이 되는지 판단할 때.

## DEC-025 — MVP 구현 언어는 TypeScript

- 상태: 확정
- 일자: 2026-08-19
- 관련 로그: LOG-20260819-001
- 관련 요구사항: REQ-013, REQ-019, REQ-020

### 결정과 이유

사용자가 앞서 제안한 TypeScript 방향이 맞는 것 같다고 수용했다. Android와 컴퓨터 웹의 공통 코드, 기능 경계의 타입 계약, 선택 가능한 얇은 서버까지 한 언어로 다룰 수 있어 솔로 개발자의 이번 주 MVP 속도에 유리하다.

### 감수하는 비용과 위험

- Android 네이티브 API에서 막히면 Kotlin 모듈을 추가해야 할 수 있다.
- 클라이언트와 서버가 같은 언어여도 실행 환경과 보안 경계까지 같아지는 것은 아니다.

### 재검토 조건

핵심 Android 기능이 TypeScript·Expo 경계에서 반복적으로 막히고 네이티브 구현 비중이 더 커질 때.

## DEC-026 — Windows Classic 계산기 이미지를 UI 참고자료로 사용

- 상태: 확정
- 일자: 2026-08-19
- 관련 로그: LOG-20260819-001
- 관련 요구사항: REQ-022
- 참고 이미지: `docs/references/2026-08-19-windows-classic-calculator.jpg`
- 해석 노트: `docs/references/windows-classic-ui.md`

### 결정과 이유

사용자가 제공한 Windows Classic 계열 계산기 화면의 “느낌”을 Inside Me UI에 참고한다. 각진 창, 선명한 파란 제목 표시줄, 회색 표면, 눌러지는 입체 버튼, 안으로 들어간 입력 영역, 제한된 색상과 격자 리듬을 디자인 DNA 후보로 사용한다.

이미지를 그대로 복제하거나 모든 화면을 계산기처럼 만들지는 않는다. 감정일기의 차분함과 모바일 사용성을 위해 본문 글꼴, 터치 영역, 대비, 화면 밀도와 내비게이션은 현대화한다.

### 감수하는 비용과 위험

- 강한 OS 모사는 감정 기록보다 장식에 시선을 빼앗거나 패러디처럼 보일 수 있다.
- 작은 픽셀 글꼴과 촘촘한 버튼 간격을 그대로 쓰면 한글 가독성과 모바일 접근성이 나빠진다.
- 제목 표시줄과 창 버튼이 실제 운영체제 기능으로 오해될 수 있다.

### 재검토 조건

첫 홈·기록·달력 목업을 실제 Android 크기로 보았을 때 감정 기록에 집중하기 어렵거나 조작 요소가 혼동될 때.

## DEC-027 — 하나의 저장소와 Expo 프로젝트 안에서 클라이언트·서버 경계를 분리

- 상태: 확정
- 일자: 2026-08-19
- 관련 로그: LOG-20260819-001, LOG-20260820-001, LOG-20260820-002
- 관련 요구사항: REQ-019, REQ-020
- 공식 근거: [Expo monorepo 공식 문서](https://docs.expo.dev/guides/monorepos/), [Expo API Routes 공식 문서](https://docs.expo.dev/router/web/api-routes/), [Expo 환경변수 보안 안내](https://docs.expo.dev/guides/environment-variables/), [Microsoft의 모놀리스 설명](https://learn.microsoft.com/en-us/dotnet/architecture/modern-web-apps-azure/common-web-application-architectures)

### 맥락

사용자는 모놀리식을 선택하면서도 프론트엔드와 백엔드를 별도 최상위 프로젝트로 나누어야 하는지 다시 확인하고 싶어 한다.

### 선택지

1. `frontend/`, `backend/`를 각각 독립 프로젝트로 만드는 한 저장소의 두 앱
2. 하나의 Expo 프로젝트 안에 얇은 화면 라우트, 기능 모듈, 플랫폼 연동과 비밀 키를 쓰는 서버 전용 코드를 구분
3. 클라이언트와 서버 코드를 구분하지 않고 같은 파일과 폴더에 섞음

### 결정과 이유

사용자가 권고안을 충분히 검토했는지 확인한 뒤 그대로 진행한다고 확정했다. 이번 주 MVP에는 2번을 사용한다. 저장소와 TypeScript 설정을 하나로 유지하고, 코드의 역할과 실행 경계만 분명히 한다. 외부 AI 비밀 키가 필요한 시점에만 Expo API Route 또는 동등한 서버 엔드포인트를 추가한다. 클라이언트 번들에는 비밀 키를 넣지 않는다.

확정 구조는 다음과 같다.

```text
inside-me/
├── app/                         # Expo Router의 얇은 화면·라우트
│   └── api/                     # 2단계에서 필요할 때만 추가; IMP-002에서는 생성하지 않음
├── src/
│   ├── features/                # home, calendar, text, voice, guided
│   ├── core/                    # 감정 탐색과 일일 기록 규칙
│   ├── infrastructure/          # storage, audio, ai, notifications
│   ├── navigation/              # 화면 사이 이동 계약
│   ├── platform/                # 파일 등 운영체제 연동
│   └── testing/                 # 공통 가짜 구현과 합성 fixture
├── server/                      # 2단계에서 필요할 때만 추가; IMP-002에서는 생성하지 않음
├── docs/
└── package.json
```

Expo 공식 문서는 여러 앱·패키지가 실제로 필요하면 monorepo와 workspaces를 지원하지만 설정 복잡도가 늘어난다고 명시한다. 지금은 별도 `apps/`와 `packages/` workspaces를 만들지 않고, 중계 서버가 독립 성장할 때 `apps/api`로 추출한다.

### 감수하는 비용과 위험

- 한 Expo 프로젝트의 API Routes를 사용하면 해당 배포 방식에 대한 의존성이 생길 수 있다.
- 서버 코드가 클라이언트에서 import되지 않도록 정적 검사와 리뷰가 필요하다.
- 나중에 서버가 커지면 별도 패키지로 이동하는 작업이 생긴다.

### 재검토 조건

서버 엔드포인트가 늘고 독립 테스트·배포·런타임이 필요해지거나, Expo API Routes가 선택한 AI·음성 처리에 맞지 않을 때.

## DEC-028 — 기능 경계는 유지하고 세로 흐름을 하나씩 완성

- 상태: 확정
- 일자: 2026-08-19
- 관련 로그: LOG-20260819-001, LOG-20260819-002, LOG-20260819-003, LOG-20260820-002
- 관련 요구사항: REQ-019, REQ-020

### 맥락

사용자는 기능별로 잘라서 구현하는 것과 전체 기능을 한꺼번에 구현하는 것 중 어느 방식이 나은지 물었다.

### 결정과 이유

사용자가 권고안을 수용하고 용어를 실제 사용 행동으로 풀어서 기록해 달라고 요청했다. 코드 구조는 기능별로 분리하되 구현은 작은 세로 흐름을 끝까지 연결해 하나씩 넓힌다. 먼저 `홈 → 글 기록 → 사용자가 감정·욕구 어휘를 직접 탐색 → 선택적 보조 후보 → 최종 확인 → 로컬 저장 → 달력과 날짜 상세`가 실제로 동작하게 한다. 그다음 같은 자기 탐색·확인·저장 핵심 흐름에 실제 AI, 자유 음성, AI 턴 대화, 음성 재생과 알림을 순서대로 붙인다.

“세로 흐름”의 사용자 행동, 저장 결과와 완료 조건은 `docs/requirements.md`와 `docs/implementation-plan.md`에 풀어서 적는다. 이 용어만으로 구현 범위를 전달하지 않는다.

달력 전체, 음성 전체, AI 전체를 각각 고립해서 완성한 뒤 마지막에 통합하면 제품의 핵심 흐름이 늦게 검증되고 통합 오류가 한꺼번에 드러난다. 반대로 모든 기능을 동시에 작성하면 실패 원인과 완료 상태를 파악하기 어렵다.

### 감수하는 비용과 위험

- 첫 세로 흐름에서는 음성·AI 턴 대화가 아직 없어 제품이 미완성처럼 보인다.
- 초기 인터페이스가 실제 공급자 연결에서 수정될 수 있다.
- 기능별 디렉터리와 세로 흐름의 공통 핵심 사이 경계를 의식적으로 지켜야 한다.

### 재검토 조건

첫 세로 흐름이 너무 얇아 핵심 가치인 AI 감정 탐색을 전혀 검증하지 못하거나 실제 공급자 제약이 데이터 모델을 크게 바꿀 때.

## DEC-029 — Android 도그푸딩은 Expo Go QR에서 시작하고 내부 APK로 전환

- 상태: 확정
- 일자: 2026-08-19
- 관련 로그: LOG-20260819-002, LOG-20260819-003
- 관련 요구사항: REQ-010, REQ-013, REQ-020
- 공식 근거: [Expo 실제 기기 실행](https://docs.expo.dev/get-started/start-developing/), [Expo 내부 배포](https://docs.expo.dev/build/internal-distribution/), [Android APK 빌드](https://docs.expo.dev/build-reference/apk/), [Expo PWA 안내](https://docs.expo.dev/guides/progressive-web-apps/)

### 맥락

사용자는 Android MVP를 만들려면 Play 스토어 배포까지 해야 해서 복잡해지는지 걱정하며, 우선 휴대폰 브라우저에서 열거나 앱처럼 설치하는 경험을 생각하고 있다.

### 선택지

1. 개발 중 Expo Go에서 QR 코드로 실행
2. EAS 내부 배포로 APK 설치 링크를 만들어 폰에 직접 설치
3. 웹사이트를 PWA로 만들어 브라우저에서 홈 화면에 설치
4. Google Play 스토어에 정식 배포

### 결정과 이유

사용자가 QR 코드로 실행하는 방식을 선택했다. 개발 중에는 Expo Go에서 QR 코드를 찍어 Android 실제 기기에 바로 실행하고, 핵심 흐름이 안정되면 EAS 내부 배포의 APK 설치 링크로 전환한다. Play 스토어 심사는 개인 도그푸딩에 사용하지 않는다.

PWA는 브라우저에서 설치할 수 있어 사용자가 처음 생각한 경험에 가깝지만, Android 네이티브 로컬 알림과 오프라인·기기 기능 검증이 핵심인 현재 제품에서는 첫 도그푸딩 경로로 권고하지 않는다. 컴퓨터 사용 경로와 보조 접근 방식으로 이후 추가한다. Play 스토어 출시는 외부 사용자 배포 준비가 끝난 뒤로 미룬다.

### 감수하는 비용과 위험

- Expo Go는 개발 컴퓨터의 서버가 켜져 있어야 하며 지원하지 않는 네이티브 모듈이 있을 수 있다.
- 내부 APK 설치는 Android가 Play 스토어 밖 앱 설치 경고를 보여 주며 새 빌드 때 다시 설치해야 한다.
- PWA를 뒤로 미루면 첫 주에는 브라우저에서 홈 화면 설치라는 사용 경험을 검증하지 못한다.
- 내부 배포 링크를 외부에 공유하면 URL을 가진 사람이 접근할 수 있으므로 개인 범위로 관리해야 한다.

### 재검토 조건

Expo Go에서 필요한 음성·알림 기능이 막힐 때, 개발 컴퓨터 없이 더 일찍 매일 사용해야 할 때, 또는 브라우저 설치 경험이 Android 네이티브보다 우선이라는 사용자 결정이 생길 때.

## DEC-030 — Android 핵심 흐름 뒤 같은 코드베이스를 PWA로 확장

- 상태: 확정
- 일자: 2026-08-19
- 관련 로그: LOG-20260819-003
- 관련 요구사항: REQ-013, REQ-024
- 공식 근거: [Expo 웹 공식 문서](https://docs.expo.dev/workflow/web/), [Expo PWA 공식 문서](https://docs.expo.dev/guides/progressive-web-apps/)

### 맥락

사용자는 Android QR 실행을 먼저 선택하면서도 휴대폰과 컴퓨터 브라우저에서 사용할 수 있고 홈 화면에 설치할 수 있는 PWA까지 확장 가능하도록 문서와 구조에 흔적을 남겨 달라고 요청했다.

### 결정과 이유

Android 핵심 흐름을 먼저 완성한 뒤 같은 Expo 코드베이스를 웹과 PWA로 확장한다. 감정 탐색 규칙, 기록 데이터 형식, 화면 컴포넌트와 기능 인터페이스를 가능한 한 공유한다. 마이크, 알림과 로컬 저장처럼 플랫폼 차이가 있는 기능은 공통 핵심에서 분리한 어댑터로 구현한다.

PWA를 첫 개발 단계에 동시에 완성하지는 않는다. Android 실제 기기의 음성·알림과 로컬 기록을 먼저 검증한 뒤, 반응형 웹, PWA manifest, 아이콘, 오프라인 시작 화면과 웹용 저장·알림 대안을 순서대로 추가한다.

### 감수하는 비용과 위험

- Android와 웹이 같은 코드를 사용해도 마이크 권한, 녹음 포맷, 알림과 저장은 별도 구현이 필요하다.
- PWA를 뒤에 붙일 때 Android 전용 가정이 남아 있으면 리팩터링 비용이 생긴다.
- 플랫폼 추상화를 너무 일찍 크게 만들면 첫 Android MVP 속도가 느려질 수 있다.
- 기록은 기기별 로컬 저장이므로 Android 앱과 PWA의 일기가 자동으로 공유되지 않는다.

### 재검토 조건

컴퓨터 웹 사용이 Android보다 중요해지거나, Expo 웹에서 핵심 음성 흐름이 충분히 동작하지 않거나, 기기 간 동기화 요구가 우선순위로 올라갈 때.

## DEC-031 — 기술 용어는 사용자 행동과 결과로 풀어서 기록

- 상태: 확정
- 일자: 2026-08-19
- 관련 로그: LOG-20260819-003
- 관련 요구사항: REQ-011, REQ-025

### 결정과 이유

`세로 흐름`, `포트·어댑터`, `모듈형 모놀리스` 같은 용어만으로 요구나 계획을 기록하지 않는다. 용어가 필요하면 함께 다음 내용을 적는다.

- 사용자가 어느 화면에서 무엇을 누르는가
- 앱이 무엇을 보여 주거나 저장하는가
- 어떤 기능이 포함되고 제외되는가
- 언제 완료로 판단하는가
- 선택의 장점, 비용과 나중에 바꿀 조건은 무엇인가

이를 통해 다음 대화에서 사용자가 문서를 읽고 자신이 의도한 제품과 같은지 직접 확인할 수 있게 한다.

### 감수하는 비용과 위험

- 문서가 길어지고 같은 개념을 요약과 상세 설명으로 두 번 표현할 수 있다.
- 구현이 바뀔 때 용어와 사용자 흐름을 함께 수정해야 한다.

### 재검토 조건

문서가 지나치게 반복되어 읽기 어려워질 때 표, 링크와 예시를 사용해 줄이되 사용자 행동 설명은 유지한다.

## DEC-032 — Inside Me를 독립 GitHub 저장소로 관리하고 문서부터 PR로 검토

- 상태: 확정
- 일자: 2026-08-19
- 관련 로그: LOG-20260819-005
- 관련 요구사항: REQ-011

### 맥락

로컬 `inside-me/`는 다른 프로젝트들이 함께 있는 상위 Git 저장소 아래의 미추적 폴더였다. 사용자는 `https://github.com/SujinHuh/inside-me`에 이 프로젝트를 연결하고, 지금까지 작성한 제품 문서와 새 README를 커밋해 PR로 올려 달라고 요청했다.

### 선택지

1. 상위 `Playground` 저장소의 일부로 계속 관리
2. `inside-me/`를 독립 Git 저장소로 초기화해 전용 GitHub 저장소에 연결
3. 문서를 더 정한 뒤 저장소 연결과 PR을 미룸

### 결정과 이유

2번을 선택한다. Inside Me 파일만 독립적으로 스테이징, 검토하고 배포할 수 있어 상위 저장소의 다른 프로젝트 변경이 섞일 위험을 줄인다.

현재 문서는 제품 목적, MVP 범위, 결정 이력, 미결정 질문과 구현 인계를 포함하므로 구현 전 제품 기준선을 남기는 첫 문서 PR로 올릴 수 있다. README에는 아직 앱 코드와 실행 명령이 없다는 사실을 명시한다. 미결정 질문이 남아 있다는 이유만으로 문서 기준선 공유를 미루지 않으며, 이후 결정은 새 커밋과 PR로 추적한다.

### 장점

- Inside Me의 변경과 이력을 다른 프로젝트와 분리할 수 있다.
- 구현 전에 제품 기준선을 PR에서 검토할 수 있다.
- README에서 현재 상태와 문서 원본 위치를 빠르게 찾을 수 있다.

### 감수하는 비용과 위험

- 상위 저장소 안에 중첩 Git 저장소가 생기므로 상위 저장소에서 Inside Me가 일반 폴더처럼 추적되지 않는다.
- 첫 문서 PR 이후 요구가 바뀌면 요구사항, 결정, 질문과 README를 함께 갱신해야 한다.
- 공개 저장소라면 제품 대화 로그와 참고 이미지도 외부에서 볼 수 있으므로 민감정보 포함 여부를 계속 검수해야 한다.

### 재검토 조건

Inside Me를 상위 저장소와 하나의 배포 또는 버전으로 관리해야 할 필요가 생기거나, GitHub 저장소의 공개 범위와 문서 공개 정책이 달라질 때.

## DEC-033 — 모든 PR에서 공통 형식과 실제 검증 결과를 기록

- 상태: 확정
- 일자: 2026-08-19
- 관련 로그: LOG-20260819-006
- 관련 요구사항: REQ-011

### 맥락

사용자는 변경 사항, 변경 내용 요약, 변경 이유, 테스트와 검증, 연관 이슈, 추가 설명과 리뷰 요청으로 구성된 기존 PR 형식을 제공하고, 저장소에 보존한 뒤 이후 모든 PR에 맞춰 작성해 달라고 요청했다.

### 선택지

1. 제공된 예시 본문을 매번 복사해 수정
2. GitHub 표준 위치에 공통 템플릿을 두고 저장소 가이드와 개발 지침에서 작성 규칙을 연결
3. 정해진 형식 없이 PR마다 자유롭게 작성

### 결정과 이유

2번을 선택한다. `.github/pull_request_template.md`를 두어 PR 작성 화면에서 자동으로 불러오고, `docs/pull-request-guide.md`에 항목별 작성 기준을 기록한다.

제공된 형식의 핵심 구성을 유지하되 Inside Me에 필요한 변경 유형, 관련 `REQ-*`·`DEC-*`·`LOG-*`, 개인정보·비밀정보와 로컬 기록 호환성 점검을 추가한다. 예시에 있던 GPT 면접 기능 같은 내용은 고정 문구로 보존하지 않고 매 PR의 실제 변경으로 교체한다.

### 장점

- PR마다 변경 이유와 검증 누락을 같은 순서로 확인할 수 있다.
- 제품 결정과 코드 변경의 근거를 추적하기 쉽다.
- 민감한 감정 기록을 다루는 제품의 개인정보와 데이터 영향을 반복해서 확인할 수 있다.

### 감수하는 비용과 위험

- 작은 문서 수정에서도 작성할 항목이 늘어난다.
- 템플릿을 기계적으로 채우면 실제 변경과 맞지 않는 체크박스나 설명이 남을 수 있다.
- 저장소 규칙이 바뀌면 템플릿, 가이드와 개발 지침을 함께 갱신해야 한다.

### 재검토 조건

PR 본문이 반복적으로 지나치게 길어 리뷰를 방해하거나, 자동화된 검사와 GitHub 이슈 연결 방식이 생겨 일부 항목을 대체할 수 있을 때.

## DEC-034 — 공통 기반 뒤 첫 세로 흐름을 세 작업으로 병렬화

- 상태: 확정
- 일자: 2026-08-19
- 관련 로그: LOG-20260819-009, LOG-20260819-010, LOG-20260819-012
- 관련 요구사항: REQ-019, REQ-020, REQ-023
- 상태 이력: LOG-20260819-009와 LOG-20260819-010에서 제안됨, LOG-20260819-012에서 확정

### 맥락

사용자는 구현 계획과 제품 문서를 바탕으로 첫 개발 업무를 여러 작업자에게 나누어 병렬로 진행하는 방법을 요청했다. 현재는 Expo 프로젝트와 앱 코드가 전혀 없어 설정, 라우트, 데이터 계약과 디자인 토큰이 먼저 필요하다.

### 선택지

1. 홈, 글 기록, 감정 확인, 달력과 상세 화면을 처음부터 각각 병렬 구현
2. 한 명이 공통 기반을 먼저 고정한 뒤 첫 세로 흐름 안의 세 기능 묶음을 병렬 구현
3. 첫 MVP 전체를 한 명이 순차 구현

### 결정과 이유

사용자가 역할 경계와 문서 구조를 상의한 뒤 이 방식으로 문서화하고 검수해 달라고 요청했다. 2번을 선택한다. 주 담당자가 Expo 스캐폴딩, 라우트, 공통 기록 타입, 저장·감정 탐색 인터페이스와 디자인 토큰을 먼저 하나의 기반 커밋으로 고정한다. 이후 다음 세 작업을 같은 기반에서 병렬로 진행한다.

1. 기록 규칙과 로컬 저장
2. 글 기록과 감정 확인
3. 월간 달력과 날짜 상세

주 담당자는 공통 설정과 계약, 최종 라우트 연결, Android 실제 기기 검증, 문서 ID와 PR 본문을 관리한다. 실제 AI, 음성, 알림과 PWA는 첫 병렬 실험에서 제외한다.

### 장점

- 기능별 모듈 경계를 지키면서 첫 완결 사용 흐름을 빠르게 연결할 수 있다.
- 공통 계약을 먼저 고정해 작업자 사이의 타입과 파일 충돌을 줄인다.
- 저장, 입력·확인, 다시 보기의 실패를 각각 집중해서 검증할 수 있다.

### 감수하는 비용과 위험

- 병렬 작업 전에 짧은 기반 단계가 필요해 즉시 여러 작업자가 코딩을 시작하지는 못한다.
- 공통 계약이 불충분하면 세 작업이 다시 멈추거나 통합 때 수정 비용이 생긴다.
- 같은 작업 디렉터리에서 Git 명령과 공통 파일을 함께 수정하면 브랜치·인덱스 충돌이 발생할 수 있다.
- 세 기능이 따로 완료되어도 주 담당자의 통합과 Android 전체 흐름 검증 없이는 MVP가 완료되지 않는다.

### 재검토 조건

첫 기반 작업이 예상보다 커져 병렬화 이점이 사라지거나, 세 기능이 공통 상태를 지나치게 자주 바꿔 독립 작업이 어렵거나, 실제 통합에서 반복적인 데이터 계약 변경이 발생할 때.

## DEC-035 — 역할과 지속 구현 루프를 별도 개발 워크플로 문서로 관리

- 상태: 확정
- 일자: 2026-08-19
- 관련 로그: LOG-20260819-011, LOG-20260819-012
- 관련 요구사항: REQ-011, REQ-019, REQ-020
- 상태 이력: LOG-20260819-011에서 제안됨, LOG-20260819-012에서 확정

### 맥락

사용자는 주 담당자와 작업자 A·B·C의 역할, 프론트엔드·앱 핵심·서버 백엔드의 단계별 경계와 반복 구현 방법을 다음 실행에서도 잃지 않도록 기록하기를 원한다. 사용자가 `시작해`라고 하면 AI가 다음 미완료 작업을 선택하고 병렬 구현, 통합, 검증과 다음 단계 인계를 반복할 수 있는 지침도 필요하다.

### 선택지

1. 모든 내용을 `AGENTS.md`에 추가
2. 모든 내용을 `docs/implementation-plan.md`에 추가
3. 상세 역할과 반복 실행 방식은 별도 개발 워크플로 문서에 두고 `AGENTS.md`와 구현 계획에서 연결

### 결정과 이유

사용자가 제안한 문서 구조로 실제 문서화를 진행하고 서브에이전트 검수를 요청했다. 3번을 선택한다.

- `docs/implementation-plan.md`는 사용자가 어떤 행동을 하고 어떤 결과가 나오면 각 단계가 완료되는지 관리한다.
- 새 `docs/development-workflow.md`는 주 담당자와 작업자 역할, 폴더 소유권, 공통 계약 고정, 작업 지시 형식, 통합 순서, 검증과 인계 형식, 반복 루프와 중단 조건을 관리한다.
- `AGENTS.md`는 매 실행에서 반드시 지킬 짧은 규칙과 두 문서의 읽기 순서를 유지한다.
- 제품 발화, 결정과 미결정 질문은 기존 `product-log.md`, `decisions.md`, `open-questions.md`의 원본 역할을 유지한다.

사용자가 개발 시작을 명시하면 AI는 현재 단계의 다음 미완료 작업을 선택하고, 안전한 로컬 구현과 검증, 관련 문서 갱신과 PR 준비까지 이어간다. 제품 범위를 바꾸는 결정, 유료·외부 서비스, 비밀정보, 실제 기기에서 사용자가 해야 하는 조작, 외부 배포·병합 또는 반복된 차단이 필요하면 멈추고 사용자에게 요청한다.

### 장점

- 구현 단계와 작업 방법이 분리되어 각 문서의 원본 책임이 선명하다.
- 새 실행에서도 같은 역할 분담과 완료 기준으로 다음 작업을 이어갈 수 있다.
- 상세 규칙이 `AGENTS.md`를 지나치게 길게 만들지 않는다.

### 감수하는 비용과 위험

- 구현 계획과 워크플로 문서를 함께 갱신해야 하는 경우가 생긴다.
- 자동 진행 규칙이 모호하면 이미 완료된 작업을 반복하거나 범위를 넘을 수 있다.
- 중단 조건을 너무 넓게 두면 자주 멈추고, 너무 좁게 두면 사용자 결정이 필요한 사항을 임의로 확정할 수 있다.

### 재검토 조건

두 문서 사이에 반복과 불일치가 자주 생기거나, 실제 개발에서 역할이 단순해져 별도 워크플로 문서가 필요하지 않게 되거나, 자동화 도구가 작업 상태와 인계를 더 정확히 관리하게 될 때.

---

## DEC-036 — 로컬 기록 내보내기를 저장소와 분리된 플랫폼 포트로 구현

- 상태: 제안됨
- 일자: 2026-08-19
- 관련 로그: LOG-20260819-012
- 관련 요구사항: REQ-015, REQ-019, REQ-020
- 관련 질문: Q-027

### 맥락

첫 로컬 MVP는 전체 기록 내보내기를 지원해야 하지만, 기록 직렬화와 Android 파일 생성·공유는 서로 다른 책임이다. 이를 기록 저장소 하나에 넣으면 도메인 규칙이 OS 파일 처리와 섞이고, 공유 취소나 실패 뒤 민감한 임시 파일이 남을 수 있다.

### 선택지

1. `EntryRepository`가 조회·저장·삭제와 파일 내보내기를 모두 담당
2. 버전 포함 직렬화와 OS 파일 생성·공유·정리를 서로 다른 계약으로 분리
3. 첫 MVP에서 내보내기를 제외

### 제안과 이유

2번을 제안한다. 작업자 A가 `schemaVersion`을 포함한 UTF-8 JSON 직렬화와 무결성을 담당하고, 주 에이전트가 별도 파일 포트로 cache 파일 생성·공유·정리를 연결하며, 작업자 C가 민감정보 안내와 사용자 진입 화면을 담당한다. 파일명에는 사용자 이름, 감정이나 일기 내용을 넣지 않는다.

### 장점

- 저장 규칙과 플랫폼 파일 API의 책임이 분리된다.
- 공유 성공·취소·실패 뒤 임시 파일 정리를 독립적으로 테스트할 수 있다.
- Android와 PWA가 같은 데이터 형식을 공유하면서 다른 파일 어댑터를 사용할 수 있다.

### 감수하는 비용과 위험

- 계약과 테스트 대역이 하나 더 필요하다.
- 앱 밖으로 공유된 파일의 수명은 앱이 통제할 수 없으므로 사용자 안내가 필요하다.
- JSON 형식의 사용성과 복구 가능성은 실제 프로토타입에서 검증해야 한다.

### 재검토 조건

도그푸딩에서 사람이 읽기 어렵거나 복원에 부족한 정보가 발견되는 경우, Android 공유 API가 cache 정리를 보장하지 못하는 경우, 암호화된 내보내기가 MVP에 필요해지는 경우 재검토한다.

---

## DEC-037 — 세 입력 방식 모두에서 사용자 자기 탐색을 AI 제안보다 먼저 제공

- 상태: 확정
- 일자: 2026-08-20
- 관련 로그: LOG-20260820-002
- 관련 요구사항: REQ-001, REQ-004, REQ-012, REQ-026
- 관련 질문: Q-016, Q-028
- 대체하는 결정: DEC-003의 AI 후보 우선 제안

### 맥락

기존 흐름은 자유 글이나 음성이 끝난 뒤 임시 탐색기 또는 AI가 감정 후보를 먼저 보여 주는 순서였다. 사용자는 자유 글, 자유 음성, AI 턴 대화 어느 방식으로 들어오더라도 AI의 해석을 먼저 받기보다 제공한 감정·욕구 목록을 직접 둘러보며 자신의 언어를 먼저 찾아보고 싶다고 요청했다.

### 선택지

1. AI가 후보를 먼저 제시하고 사용자가 선택
2. 사용자가 어휘를 먼저 탐색하고 원할 때만 AI가 보조 후보를 제안
3. AI와 사용자 선택을 한 화면에 동시에 제시

### 결정과 이유

2번을 선택한다. 세 입력 방식은 입력 과정만 다르고, 입력이 끝난 뒤에는 같은 자기 탐색 화면과 핵심 계약을 사용한다.

1. 사용자가 감정 목록을 검색·분류·스크롤하며 먼저 선택하거나 직접 추가한다.
2. 욕구 목록을 주제별로 둘러보고 자신에게 중요했던 항목을 선택한다.
3. 사용자가 원하면 AI가 이야기와 자기 선택을 바탕으로 추가 후보와 차이를 설명한다.
4. 사용자가 최종 감정·욕구와 강도를 수정·확정한다.

고정 이미지는 초기 어휘와 정보 구조의 참고자료로 보존하지만, 기본 선택 UI는 접근 가능한 구조화 텍스트로 구현한다.

### 장점

- AI의 첫 제안이 사용자의 자기 인식을 고정하는 앵커링을 줄인다.
- AI 연결 전에도 첫 로컬 MVP의 감정 탐색 가치가 작동한다.
- 세 입력 방식이 같은 감정·욕구 어휘, 선택 상태와 저장 계약을 재사용한다.
- 검색, 직접 추가, 스크린 리더와 선택 상태 표시를 지원하기 쉽다.

### 감수하는 비용과 위험

- 긴 어휘 목록이 선택 피로를 만들 수 있다.
- 감정·욕구 분류와 검색용 구조화 데이터 준비가 첫 MVP 범위에 추가된다.
- 사용자가 무엇을 골라야 할지 모를 때 AI 도움까지 한 단계 더 필요해질 수 있다.
- 욕구 충족 여부 분류가 감정의 객관적 원인이나 좋고 나쁨으로 오해되지 않게 문구를 검증해야 한다.

### 재검토 조건

Android 도그푸딩에서 목록 탐색을 반복적으로 건너뛰거나, 선택 시간이 지나치게 길거나, 사용자가 AI 도움을 항상 먼저 요청하는 경우 탐색 단계의 밀도와 진입 방식을 재검토한다.

---

## DEC-038 — 모든 구현 단위 후 서브에이전트 독립 검수를 다음 구현의 필수 게이트로 사용

- 상태: 확정
- 일자: 2026-08-20
- 관련 로그: LOG-20260820-005, LOG-20260820-006
- 관련 요구사항: REQ-019, REQ-020, REQ-023
- 관련 질문: 없음

### 맥락

IMP-002A·IMP-002B의 Expo 기반은 주 에이전트가 설치, 호환성, 타입, 린트, 번들과 로컬 서버를 자동 검증했지만 서브에이전트가 구현 diff를 별도로 검수하지는 않았다. 사용자는 앞으로 하나의 구현이 끝나면 서브에이전트가 검수하고, 오류가 없을 때만 다음 구현으로 넘어가는 규칙을 지속 실행 문서에 추가하도록 요청했다.

### 선택지

1. 주 에이전트의 자동 검증만 통과하면 다음 구현으로 진행
2. 모든 구현 단위마다 구현에 참여하지 않은 서브에이전트가 독립 검수하고 오류 해소 후에만 진행
3. 크거나 위험한 변경만 주기적으로 모아 검수

### 결정과 이유

2번을 선택한다. 가장 작은 세로 조각 또는 원자 작업 ID 단위의 구현·통합·자동 검증이 끝나면 상태를 `독립 검수 대기`로 바꾸고 다음 구현을 멈춘다. 구현에 참여하지 않은 서브에이전트가 기준 SHA와 diff를 읽기 전용으로 검수한다.

서브에이전트가 확인한 오류가 있으면 주 에이전트가 수정·자동 재검증·재검수한다. 확인된 오류가 0개이고 미검증 범위와 남은 제안·위험을 기록했을 때만 현재 구현을 `완료`로 바꾸고 다음 구현으로 넘어간다. 독립 검수를 실행할 수 없는 환경에서는 주 에이전트의 자체 리뷰로 대체하지 않는다.

### 검수 범위와 통과 증거

- 요구사항·결정·사용자 행동 일치
- 정상·실패·취소·재시작과 데이터 보존
- 계층·공통 계약·플랫폼 경계
- 비밀정보·개인정보·로그·삭제·내보내기·마이그레이션 안전
- 테스트의 핵심 회귀·실패 경로 커버와 미검증 범위
- UI의 Android 화면, 키보드, 스크린 리더, 색상 외 표시와 디자인 원칙
- 기준·대상 SHA 또는 diff, 검수 관점, 발견·수정·재검수 결과

### 장점

- 구현자의 가정이 자동 검증과 자체 리뷰를 동시에 통과하는 오판을 줄인다.
- 오류가 뒤 단계에 누적되기 전에 작은 diff에서 수정할 수 있다.
- 다음 실행이 어떤 근거로 시작되었는지 SHA와 검수 결과로 재현할 수 있다.

### 감수하는 비용과 위험

- 매 구현 단위마다 검수 시간이 추가되어 순수 구현 속도는 느려질 수 있다.
- 검수 단위가 너무 크면 오류 위치가 흐려지고, 너무 작으면 검수 전환 비용이 커진다. 따라서 사용자 행동을 한 칸 앞으로 옮기는 가장 작은 완결 단위로 나눈다.
- 서브에이전트의 제안을 모두 오류로 취급하면 불필요한 재작업이 발생할 수 있으므로 근거가 있는 오류와 취향·후속 제안을 구분한다.

### 재검토 조건

초소형 변경에서 검수 전환 비용이 지속적으로 구현 비용보다 커지거나, 독립 검수 결과가 장기간 중복만 발생하거나, 에이전트를 사용할 수 없는 환경이 반복되면 검수 단위를 묶는 기준만 재검토한다. 확인된 오류 0개 전에 다음 구현을 막는 원칙은 유지한다.

---

## DEC-039 — 저장소의 Expo CLI 명령에서 익명 텔레메트리를 기본 비활성화

- 상태: 제안됨
- 일자: 2026-08-20
- 관련 로그: LOG-20260820-007
- 관련 요구사항: REQ-019, REQ-020
- 관련 질문: 없음

### 맥락

IMP-002R 보안·개인정보 독립 검수에서 Expo CLI가 기본적으로 익명 사용량 텔레메트리를 수집할 수 있다는 점이 의존성 데이터 전송 기록에서 빠진 것을 확인했다. 감정일기 원문이 전송되는 기능은 아니지만, 개인정보 우선 개발 환경에서는 개발 도구의 외부 전송도 명시적으로 다루는 편이 제품 원칙과 맞다.

### 선택지

1. 기본 텔레메트리를 유지하고 문서에만 고지
2. 개발자 전역 설정으로 비활성화
3. 저장소의 모든 Expo 명령에서 `EXPO_NO_TELEMETRY=1`을 강제

### 제안과 이유

3번을 제안하고 되돌리기 쉬운 기본안으로 적용한다. `scripts/expo-cli.cjs`가 추가 의존성 없이 Expo CLI를 실행하면서 텔레메트리 비활성화 환경 변수를 넣는다. 따라서 운영체제별 환경 변수 문법에 의존하지 않고 `npm start`, Expo 호환성 검사와 웹 번들처럼 Expo CLI를 사용하는 package script에 같은 정책을 적용할 수 있다. 린트는 텔레메트리 기능이 없는 ESLint를 직접 실행한다.

### 장점

- 개발 도구의 예상하지 못한 외부 사용량 전송을 기본 차단한다.
- 개발자 전역 설정을 바꾸지 않고 저장소 안에서 재현된다.
- 외부 패키지를 추가하지 않는다.

### 감수하는 비용과 위험

- Expo CLI 직접 호출과 package script의 동작 정책이 달라질 수 있으므로 공식 실행 경로를 package script로 유지해야 한다.
- Expo가 CLI 진입점이나 환경 변수 정책을 바꾸면 래퍼를 갱신해야 한다.

### 재검토 조건

Expo가 텔레메트리를 기본 비활성화하거나 해당 환경 변수 지원을 중단하거나, 래퍼가 SDK 업그레이드를 방해하면 공식 최신 방식으로 교체한다.

---

## DEC-040 — IMP-003 테스트 기반은 Jest Expo와 명시적으로 분리된 기록 계약을 사용

- 상태: 제안됨
- 일자: 2026-08-20
- 관련 로그: LOG-20260820-008
- 관련 요구사항: REQ-004, REQ-018, REQ-019, REQ-023, REQ-026
- 관련 질문: 없음

### 맥락

첫 기록 흐름을 병렬 구현하기 전에 화면과 저장 구현이 함께 사용할 날짜, 기록, 감정·욕구 선택, AI 보조, 저장 오류와 내보내기 경계를 고정하고 회귀 테스트를 실행할 기반이 필요하다.

### 제안과 이유

- Expo 공식 가이드와 SDK 54에 맞는 `jest-expo`와 Jest 29를 사용한다.
- React Native 화면 테스트를 위해 `@testing-library/react-native`를 개발 의존성으로 두되 React와 renderer를 19.1.0으로 정확히 맞춘다.
- 기록 계약은 `userExpressed`, `userSelected`, `aiSuggested`, `finalConfirmed`를 분리하고 AI 제안이 최종 선택으로 자동 승격되지 않게 한다.
- 감정과 욕구 선택은 서로 다른 구별 타입으로 두고, 날짜 키·월 키·시간은 포트 뒤에 둔다.
- 저장 결과는 생성과 갱신을 구분하고 민감한 원문이 없는 안전한 오류 코드·문구를 반환한다.

### 장점

- 같은 날짜를 다시 저장할 때 중복 생성되는 회귀를 화면 구현 전에 잡는다.
- A·B·C 작업자가 같은 계약을 읽고 독립적으로 구현할 수 있다.
- 실제 저장소 없이도 합성 데이터로 화면 흐름을 검증할 수 있다.

### 감수하는 비용과 위험

- 개발 의존성과 audit 중간 위험 1건이 추가된다.
- 계약을 너무 일찍 넓히면 사용하지 않는 필드가 굳을 수 있어 첫 세로 흐름에 필요한 값만 유지해야 한다.
- 날짜 키는 생성·파싱 정책을 통해서만 얻는 불투명 타입으로 두지만, 외부 저장 데이터가 타입 검사를 우회할 수 있으므로 저장 경계의 런타임 검증을 계속 유지해야 한다.

### 재검토 조건

SDK 업그레이드로 Jest Expo 호환성이 깨지거나, 실제 화면 테스트에서 다른 공식 도구가 더 단순하거나, 첫 기록 흐름에서 계약이 사용자 행동을 표현하지 못하면 IMP-003 안에서 변경한다.

---

## DEC-041 — 클린 코드 지침을 모든 구현과 독립 검수의 반복 게이트로 사용

- 상태: 확정
- 일자: 2026-08-20
- 관련 로그: LOG-20260820-009
- 관련 요구사항: REQ-019, REQ-020, REQ-023
- 관련 질문: 없음

### 맥락

IMP-003A 완료 뒤 클린 코드와 큰 틀 아키텍처를 다시 검수하자 core를 향한 의존성 방향과 계약 분리는 양호했지만, 일부 필드만 검사한 원본 객체를 저장하는 런타임 경계, 실제 Expo Router 파일과 연결되지 않은 수동 라우트 목록, 한 파일에 누적된 테스트 책임이 확인됐다. 사용자는 이번 수정에 그치지 않고 다음 개발에서도 같은 기준을 계속 적용할 수 있도록 docs에 정리하고 코드에 반영하도록 요청했다.

### 선택지

1. 이번 발견만 코드에서 수정하고 별도 지침은 만들지 않음
2. 일반적인 클린 코드 원칙만 링크하고 구현자가 해석함
3. Inside Me의 계층·민감 데이터·Expo Router·테스트 구조에 맞는 구체 지침을 만들고 구현·독립 검수 게이트에 연결함

### 결정과 이유

3번을 선택한다. `docs/clean-code-guidelines.md`를 반복 품질 원본으로 두고, 구현 전 책임 확인, 자동 검증 전 체크리스트, 서브에이전트 독립 검수 관점에 연결한다.

저장·JSON·마이그레이션 경계에서는 전체 구조를 검사하고 허용 필드만 새 객체로 투영한다. `app/` 라우트는 실제 feature 화면과 조합 루트를 연결하는 얇은 진입점으로 유지하고, Expo Router의 실제 파일과 typed route를 단일 원본으로 사용한다. fixture, CRUD, 경계 검증과 실패 주입은 책임별 테스트로 나누고 ID·시간·clone이 실제 계약을 왜곡하지 않게 한다.

### 장점

- 이름과 실제 책임이 달라 생기는 잘못된 안전감을 줄인다.
- 타입을 우회한 외부 데이터와 추가 민감 필드가 저장·내보내기에 섞이는 것을 경계에서 막는다.
- 기능이 늘어나도 core, feature, platform과 route 의존성 방향을 반복해서 확인할 수 있다.
- 테스트 실패 원인과 누락된 데이터 보존 경로를 더 빠르게 찾을 수 있다.

### 감수하는 비용과 위험

- 작은 구현마다 체크리스트와 검수 시간이 추가된다.
- 모든 취향 차이를 현재 오류로 취급하면 사용자 흐름 구현이 느려질 수 있다. 확인된 오류와 후속 리팩터링 제안을 분리한다.
- parser와 contract test가 늘어나므로 실제 사용하는 데이터 범위를 넘겨 과도하게 일반화하지 않는다.

### 재검토 조건

지침이 실제 오류를 잡지 못하고 형식적인 체크로만 반복되거나, 기능별 하위 지침과 충돌하거나, 저장 스키마·라우팅 기술이 바뀌면 항목과 적용 위치를 갱신한다. 민감 데이터 경계의 전체 parser와 얇은 route 원칙은 대체 설계가 동일한 안전성을 증명하기 전까지 유지한다.

---

## DEC-042 — 완료한 기능 단위는 push와 PR 생성·갱신까지 지속 구현 루프에 포함

- 상태: 확정
- 일자: 2026-08-20
- 관련 로그: LOG-20260820-010
- 관련 요구사항: REQ-019, REQ-020, REQ-023
- 관련 질문: 없음

### 맥락

기존 루프는 구현·검증·독립 검수·로컬 커밋까지 진행하고 push와 PR은 매 요청에서 별도로 확인했다. 사용자는 각 기능이 끝날 때마다 push와 PR을 함께 올리고, 저장소 PR 템플릿을 실제 변경에 맞게 작성하는 과정을 지속 루프에 포함하도록 요청했다.

### 선택지

1. 기능을 완료해도 로컬 커밋에서 멈추고 매번 push·PR 요청을 다시 받음
2. 기능 완료 뒤 현재 브랜치 push만 자동 수행하고 PR은 별도 요청을 받음
3. 기능 완료·독립 검수·커밋 뒤 현재 브랜치를 push하고 같은 브랜치의 PR을 생성하거나 갱신함

### 결정과 이유

3번을 선택한다. 구현 단위의 자동 검증과 서브에이전트 독립 검수에서 확인된 오류가 0개이면 구현과 관련 문서를 기능 커밋 SHA A로 고정한다. 원장에 SHA A를 기록해 상태 문서 커밋 SHA B를 만들고 clean 상태를 확인한 뒤 현재 `inside-me` 개발 브랜치를 origin에 push한다. 같은 head 브랜치의 PR이 없으면 `main` 대상으로 만들고, 이미 있으면 중복 생성하지 않고 실제 변경·검증·미검증 범위에 맞게 갱신한다.

모든 PR은 `.github/pull_request_template.md`와 `docs/pull-request-guide.md` 형식을 따른다. `main...head` 전체 PR 범위의 모든 완료 기준·자동 검증·독립 검수가 통과하고 미완료 항목이 없을 때만 병합 가능한 PR로 표시한다. 하나라도 남으면 Draft를 유지한다. PR 병합·배포·force push는 이 결정에 포함하지 않는다.

### 장점

- 기능 완료 결과와 검증 근거가 GitHub에 바로 남아 다음 리뷰와 재개가 쉬워진다.
- 로컬에만 쌓인 커밋이 원격에 누락되는 일을 줄인다.
- 모든 기능 PR에서 개인정보·데이터 영향과 미검증 범위를 같은 형식으로 확인한다.

### 감수하는 비용과 위험

- 작은 기능마다 원격 push와 PR 갱신이 발생한다.
- 한 브랜치에서 여러 기능을 이어가면 PR 범위가 커질 수 있으므로 완료 단위가 달라지면 새 작업 브랜치를 사용한다.
- GitHub 인증·네트워크 실패가 발생하면 로컬 구현은 완료됐어도 원격 게이트에서 멈출 수 있다.

### 재검토 조건

PR 갱신이 너무 잦아 리뷰 단위가 흐려지거나, 기능마다 별도 브랜치가 더 적합하거나, 자동 원격 변경이 협업 흐름을 방해하면 브랜치·PR 단위를 조정한다. 병합·배포를 별도 승인으로 두는 경계는 유지한다.

---

## DEC-043 — PR 제목과 본문은 한국어로 작성

- 상태: 확정
- 일자: 2026-08-20
- 관련 로그: LOG-20260820-011
- 관련 요구사항: 없음
- 관련 질문: 없음

### 맥락

현재 PR 본문은 한국어 템플릿을 사용하지만 제목에는 영문 Conventional Commit 접두어가 붙어 있었다. 사용자는 현재 PR 제목을 한국어로 바꾸고, 이후 PR도 제목과 본문을 한국어로 작성하도록 문서에 고정해 달라고 요청했다.

### 선택지

1. 본문만 한국어로 쓰고 제목 형식은 작성자에게 맡김
2. 제목과 본문을 한국어로 쓰되 코드 식별자·명령·파일명은 원문을 유지함
3. 코드 식별자와 명령까지 모두 한국어로 번역함

### 결정과 이유

2번을 선택한다. PR 제목에는 `feat:`, `docs:` 같은 영문 Conventional Commit 접두어를 붙이지 않고 변경 결과를 짧은 한국어 명사형으로 적는다. PR 본문도 한국어 템플릿에 맞춰 실제 변경·검증·미검증·문서 연결과 개인정보 영향을 한국어로 설명한다. 코드 식별자·명령·파일명은 정확성을 위해 원문을 유지할 수 있다.

### 장점

- 저장소의 주요 협업 문맥을 한국어로 일관되게 읽을 수 있다.
- 제목만 보고도 변경 결과를 바로 파악하기 쉽다.
- 기술 식별자를 억지로 번역해 의미가 흐려지는 일을 피한다.

### 감수하는 비용과 위험

- Conventional Commit 형식의 PR 제목을 전제로 하는 자동화가 생기면 별도 설정이 필요하다.
- 영문 라이브러리명과 명령이 본문에 일부 남을 수 있다.

### 재검토 조건

릴리스 자동화가 PR 제목의 Conventional Commit 접두어를 요구하거나 외부 협업자가 공통 영문 형식을 요구하면 제목 규칙을 다시 검토한다. 본문에서 실제 검증과 개인정보 영향을 명시하는 원칙은 유지한다.

---

## DEC-044 — IMP-003B를 구조화 어휘 기반과 앱 조합 기반으로 나누어 구현

- 상태: 제안됨
- 일자: 2026-08-20
- 관련 로그: LOG-20260820-012
- 관련 요구사항: REQ-004, REQ-018, REQ-019, REQ-023, REQ-026
- 관련 질문: Q-028

### 맥락

IMP-003B에는 구조화 어휘, 가짜 탐색기, 조합 루트, 공통 상태 UI와 실제 로컬 저장 어댑터 선택이 함께 들어 있었다. 한 조각에서 모두 바꾸면 감정·욕구 데이터 계약과 React Native 화면·저장 의존성의 실패 원인이 섞이고 독립 검수 범위가 커진다.

### 제안과 이유

IMP-003B1에서는 사용자 참고 이미지에서 모바일 프로토타입에 필요한 대표 어휘만 안정 ID·주제·검색어·선택적 탐색 태그로 옮기고, 사용자가 명시적으로 도움을 요청했을 때만 호출할 결정적 가짜 탐색기를 만든다. 가짜 탐색기는 사용자가 이미 고른 항목을 제외하고 종류별 최대 3개만 제안하며 최종 확정값을 만들지 않는다.

IMP-003B2에서는 이 기반을 실제 조합 루트와 공통 상태 UI에 연결하고 Expo Go 호환 로컬 저장 어댑터를 선택한다. 어휘 시드는 완전한 정답표가 아니라 프로토타입용 시작점이며 자유 입력과 `아직 모르겠어요` 경로는 화면 단계에서 반드시 제공한다.

### 장점

- 어휘 검색·분류와 AI 비자동확정 규칙을 화면·저장 기술과 분리해 검증할 수 있다.
- 작업자 B가 동일한 안정 ID와 주제를 사용해 화면을 만들 수 있다.
- 긴 목록의 실제 밀도는 Android 프로토타입에서 조정하되 데이터 계약을 다시 만들 필요가 줄어든다.

### 감수하는 비용과 위험

- IMP-003B 안에 사용자 체크포인트가 하나 늘어난다.
- 초기 시드는 참고 이미지의 모든 단어를 포함하지 않으며 일부 표현과 분류는 바뀔 수 있다.
- 키워드 기반 가짜 탐색기는 실제 AI 품질을 대표하지 않으며 제품 외부에 노출하면 안 된다.

### 재검토 조건

Android 프로토타입에서 사용자가 원하는 표현을 찾기 어렵거나 주제 구분이 선택을 방해하면 Q-028 결과에 따라 시드·주제·검색어를 바꾼다. 실제 AI 단계에서는 동의·외부 전송·취소·응답 스키마와 안전 검증을 별도로 결정한다.

---

## DEC-045 — Android 로컬 기록 기반으로 Expo SQLite를 사용

- 상태: 제안됨
- 일자: 2026-08-20
- 관련 로그: LOG-20260820-014
- 관련 요구사항: REQ-015, REQ-019, REQ-020, REQ-023
- 관련 질문: Q-004, Q-027

### 맥락

첫 기록 흐름은 하루 한 기록, 날짜·월별 조회, 수정·삭제, 전체 내보내기와 후속 schema migration을 기기 안에서 처리해야 한다. IMP-003B2에서는 작업자 A가 실제 repository를 구현하기 전에 Expo Go에서 사용할 로컬 저장 기술과 조합 경계를 정해야 한다.

### 선택지

1. 메모리 저장소만 유지해 화면을 먼저 만들고 재시작 복원은 나중에 추가
2. 단순 key-value 저장소에 전체 기록 JSON을 넣음
3. Expo Go에 포함된 `expo-sqlite`를 네이티브 구조화 저장 기반으로 선택하고 repository port 뒤에 둠

### 제안과 이유

3번을 되돌리기 쉬운 기본안으로 적용한다. SQLite는 날짜 key의 유일성, 월별 조회, transaction과 user version 기반 migration을 표현하기 쉽고 Expo SDK 54의 Expo Go에 포함된다. `src/composition/`에서 네이티브 Provider를 연결하고 `src/infrastructure/storage/`에서 WAL·외래 키만 준비한다. 실제 기록 table, CRUD, 손상 데이터와 migration은 IMP-101에서 전체 parser와 repository contract test를 함께 적용한다.

웹 SQLite는 공식 문서상 alpha이며 WASM과 서버 header 설정이 필요하므로 이번 네이티브 선택을 PWA에 그대로 강제하지 않는다. 웹은 현재 영속 저장 없이 번들을 유지하고 6단계에서 별도 저장 adapter를 결정한다.

### 장점

- 하루 한 기록, 월별 달력 조회와 원자적 수정·삭제를 구조화된 제약으로 구현할 수 있다.
- Expo Go Android 도그푸딩과 호환되고 별도 서버·계정·외부 전송이 없다.
- core repository port가 저장 기술을 가려 이후 암호화·웹 저장 변경 범위를 줄인다.

### 감수하는 비용과 위험

- 새 네이티브 런타임 패키지와 schema migration 책임이 생긴다.
- 운영체제 app sandbox 외의 앱 계층 암호화는 아직 없으며 기기 위협 모델 확대 전에 보강해야 한다.
- 웹은 Android와 같은 저장 구현을 즉시 공유하지 못하고 PWA 단계에서 별도 검증이 필요하다.

### 재검토 조건

Android 실기기에서 Expo Go DB 초기화·복원이 불안정하거나, 기록 암호화 요구가 커지거나, PWA 우선순위가 올라가 웹 alpha 제약을 감수할 이유가 생기면 저장 adapter와 암호화 방식을 다시 검토한다. 사용자가 이 기본안을 확정하기 전까지 상태는 `제안됨`으로 유지한다.

---

## DEC-046 — 달력 얼굴은 대표 감정을 중심으로 표현

- 상태: 확정
- 일자: 2026-08-23
- 관련 로그: LOG-20260823-005, LOG-20260823-006
- 관련 요구사항: REQ-007, REQ-008, REQ-018, REQ-027
- 관련 질문: Q-008

### 맥락

월간 달력의 작은 날짜 셀은 한눈에 읽혀야 하지만, 사용자는 소수의 기분 단계보다 최대한 다양한 감정이 드러나기를 원한다. 하루에는 여러 감정이 함께 있을 수 있으므로 얼굴 하나가 하루 전체를 객관적으로 판정하는 것처럼 보여서도 안 된다.

### 선택지

1. 대표 감정: 사용자가 최종 선택한 감정 중 달력에서 먼저 기억하고 싶은 하나
2. 편안함 정도: 편안함에서 불편함까지의 단계
3. 감정 강도: 약함에서 강함까지의 단계

### 선택과 이유

사용자가 권고안을 수용해 1번으로 확정했다. 달력 얼굴은 사용자가 직접 고른 대표 감정을 빠르게 떠올리는 표식으로 사용한다. 정확한 대표 감정 이름을 날짜 셀에서 읽을 수 있게 하고, 추가 감정이 있으면 작은 보조 표시를 둔다. 날짜 상세에서는 대표 감정뿐 아니라 사용자가 확정한 모든 감정 이름과 각각의 강도를 그대로 보여준다.

표정 세트는 운영체제 이모지나 좋음·보통·나쁨 몇 단계에 의존하지 않는다. 현재 11개 감정 주제를 탐색과 시각 체계의 시작점으로 삼되, 실제 저장 단위는 `기쁨과 즐거움` 같은 넓은 주제가 아니라 `감사한`, `서운한`, `억울한`처럼 사용자가 선택한 구체적 감정이다. 목록에 없는 감정의 직접 추가도 유지한다. 편안함 정도와 강도는 얼굴의 중심 의미가 아니라 표정의 작은 변형이나 상세 정보로 보조한다.

### 장점

- 사용자가 그날을 어떤 감정으로 기억했는지 달력에서 바로 떠올릴 수 있다.
- 구체적인 감정 이름과 복수 감정을 보존해 감정을 몇 개의 기분 단계로 축약하지 않는다.
- 대표 감정, 편안함과 강도의 책임을 분리해 달력 아이콘 계약이 명확해진다.

### 감수하는 비용과 위험

- 다양한 감정을 작은 얼굴로 모두 구별하기 어려워 전용 표정 체계와 텍스트 레이블이 함께 필요하다.
- 사용자가 대표 감정 하나를 고르는 단계가 추가된다.
- 비슷한 얼굴이 생길 수 있으므로 색상만으로 구분하지 않고 이름과 형태를 함께 검증해야 한다.

### 재검토 조건

Android 달력 프로토타입에서 얼굴 간 구분이 어렵거나 대표 감정 선택이 부담스럽다는 결과가 반복되면 편안함·활력 좌표 또는 복수 감정 표시를 중심으로 다시 설계한다.

---

## DEC-047 — IMP-106을 대기로 유지하고 공급자 독립 AI 경계를 먼저 구현

- 상태: 확정
- 일자: 2026-08-24
- 관련 로그: LOG-20260824-001, LOG-20260824-002
- 관련 요구사항: REQ-009, REQ-019, REQ-020, REQ-026
- 관련 질문: Q-013

### 맥락

첫 기록 흐름의 자동 검증과 독립 검수는 끝났지만 Android Expo Go에서 작성·재시작 복원·내보내기·삭제를 확인하는 IMP-106은 사용자가 폰을 직접 조작해야 한다. 사용자는 현재 외부에서 Codex를 원격으로 사용해 기기 확인이 어렵지만, 그동안 진행할 수 있는 다른 개발을 요청했다.

### 선택지

1. Android 확인이 가능할 때까지 모든 개발을 중단
2. IMP-106을 완료로 간주하고 실제 AI·음성·알림 구현을 계속 누적
3. IMP-106은 대기로 유지하고 기기 성공과 외부 공급자를 전제로 하지 않는 계약·합성 테스트만 먼저 구현

### 선택과 이유

사용자가 권고안을 수용하고 원격 개발 진행을 요청해 3번으로 확정한다. 첫 조각은 실제 AI 공급자를 고르지 않고 최소 전송 필드와 transport port, 외부 응답의 런타임 검증·기존 어휘 매핑, 안전한 오류만 구현한다. 프로덕션 조합에는 연결하지 않으므로 실제 일기 내용은 외부로 전송되지 않는다.

### 장점

- 사용자가 폰을 사용할 수 없는 시간에도 되돌리기 쉬운 기반 작업을 진행할 수 있다.
- 공급자를 고르기 전에 데이터 최소화, 오류와 응답 스키마를 테스트로 고정할 수 있다.
- IMP-106 실패가 발견돼도 실제 네트워크·음성·알림 문제와 섞이지 않는다.

### 감수하는 비용과 위험

- 실행 원장에 Android 대기 포인터와 원격 독립 포인터가 잠시 함께 존재한다.
- 계약이 실제 공급자 제약과 맞지 않아 Q-013 결정 뒤 일부 수정될 수 있다.
- 실제 사용자 가치와 Android 화면은 이번 조각만으로 늘어나지 않는다.

### 재검토 조건

사용자가 Android 폰을 사용할 수 있게 되면 IMP-106을 새 플랫폼 기능보다 먼저 재개한다. 실제 공급자 후보의 데이터 정책·응답 형식·비용이 현재 계약과 맞지 않거나 사용자 동의 범위가 달라지면 실제 연결 전에 계약을 재검토한다.
