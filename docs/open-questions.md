# Inside Me 미결정 질문

이 문서는 개발을 시작하기 전에 답이 필요한 질문과 프로토타입을 사용한 뒤 결정할 질문을 분리한다. 사용자가 답하면 관련 질문을 `답변됨`으로 바꾸고 `requirements.md`와 `decisions.md`에 반영한다.

## 상태 정의

- `답변 필요`: 구현 방향을 크게 바꾸므로 개발 전에 답이 필요함
- `기본안으로 진행 가능`: 권고 기본안으로 프로토타입을 만든 뒤 바꿀 수 있음
- `프로토타입 후 검증`: 실제 사용 경험을 보고 결정하는 편이 나음
- `부분 답변됨`: 큰 방향은 확정됐지만 프로토타입으로 세부 값을 검증해야 함
- `답변됨`: 결정 로그에 반영됨

## 먼저 답이 필요한 질문

### Q-015 — “두 가지 버전”의 의미

- 상태: 답변됨 → DEC-011
- 영향: 화면 흐름과 개발 범위
- 답변: 한 앱 안에 자유 음성 기록, 글 기록, AI 턴 대화의 세 가지 경험을 둔다.
- 후속 확인: 자유 기록이 끝난 뒤 AI 질문으로 전환되는 정확한 방식은 프로토타입 설계 때 확인한다.

### Q-001 — 실제로 사용할 기기와 운영체제

- 상태: 답변됨 → DEC-012
- 영향: 앱 형태, 알림, 마이크 권한, 배포와 테스트 방법
- 답변: 안드로이드폰을 주로 사용하고 컴퓨터도 사용한다. 아이폰은 사용하지 않는다.
- 이유: 음성과 취침 전 알림은 모바일 운영체제의 제약을 크게 받으므로 기술 스택보다 실제 사용 기기를 먼저 알아야 한다.

### Q-002 — AI의 답변을 듣는 방식

- 상태: 답변됨 → DEC-013
- 영향: 음성 합성, 비용, 응답 지연, 화면 구성
- 답변: AI의 공감 표현과 질문도 목소리로 듣고 싶다. 구체적인 재생 방식은 직접 사용한 뒤 AI의 추천을 받아 결정한다.
- 운영 기본안: AI 질문은 음성과 글을 동시에 제공하고 언제든 음소거할 수 있게 한다.

### Q-003 — 한 턴의 녹음 조작 방식

- 상태: 답변됨 → DEC-015
- 영향: 음성 UI, 발화 종료 감지, 오전사 수정, 접근성
- 질문 의도: 사용자가 버튼을 눌러 녹음을 시작하고 다시 눌러 끝낼지, 앱이 침묵을 감지해 자동으로 한 턴을 끝낼지를 묻는 질문이다.
- 답변: 버튼을 한 번 눌러 시작하고 다시 눌러 종료하는 권고안을 수용했다. 감정을 정리하는 중간 침묵을 발화 종료로 오해할 수 있으므로 첫 MVP에서는 자동 종료하지 않는다.

### Q-004 — 기록 저장 위치와 동기화

- 상태: 답변됨 → DEC-014
- 영향: 개인정보, 백엔드, 로그인, 기기 분실 시 복구
- 답변: 나중에는 다른 기기에서도 이어서 보고 싶지만 첫 버전은 현재 기기에만 저장한다. 기록 동기화 서버는 지금 만들지 않는다.
- 운영 기본안: 내보내기와 삭제를 지원하고 AI 처리에 필요한 최소 텍스트만 외부로 전송한다.

### Q-005 — 원본 음성 보관

- 상태: 답변됨 → DEC-016
- 영향: 개인정보, 저장 공간, 재생 기능, 전사 재처리
- 답변: 목소리를 다시 들을 필요가 없으므로 원본 음성은 전사와 분석에 사용한 뒤 삭제한다. 텍스트와 감정 결과만 저장한다.

### Q-006 — 한 번의 감정 탐색 길이

- 상태: 답변됨 → DEC-017
- 영향: 질문 수, 비용, 취침 전 피로, 완료율
- 답변: 약 5분 안에 핵심 질문 3~6개를 진행하고 사용자가 원하면 더 이어가는 권고안을 수용했다.

### Q-007 — 하루 기록 개수

- 상태: 답변됨 → DEC-018
- 영향: 데이터 모델, 달력의 대표 표정, 알림 완료 처리
- 답변: MVP에서는 하루의 내용을 한 기록으로 묶어 정리한다. 사건별로 나누어 저장하는 기능은 나중에 확장한다.

## 프로토타입을 본 뒤 결정할 질문

### Q-008 — 월간 달력의 표정이 나타낼 정보

- 상태: 답변됨 → DEC-046
- 영향: 아이콘 체계와 하루의 복합 감정 표현
- 질문: 달력에서 얼굴 하나를 봤을 때 가장 먼저 기억하고 싶은 것은 대표 감정, 기분의 편안함 정도, 감정의 강도 중 무엇인가?
- 답변: 사용자가 직접 고른 대표 감정을 얼굴의 중심 의미로 삼는다. 날짜 셀에는 정확한 감정 이름과 추가 감정 존재를 보조 표시하고, 상세 화면에서는 복수 감정과 각각의 강도를 유지한다. 편안함 정도와 강도는 대표 의미가 아니라 얼굴의 작은 시각 변형 또는 상세 정보로 사용한다.
- 검증 항목: 제공된 다른 앱 화면은 정보 구조와 표정 다양성만 참고하고 그대로 복제하지 않는다. Android 프로토타입에서 전용 표정의 구분 가능성, 감정 이름 가독성, 추가 감정 표시와 접근성을 검증한다.

### Q-009 — 이전 기록을 AI가 기억하는 범위

- 상태: 프로토타입 후 검증
- 영향: 개인화, 비용, 프라이버시, 잘못된 패턴 단정
- 질문: AI가 이전 일기를 참고해 반복되는 감정과 욕구를 연결해 주길 원하는가? 원한다면 어느 기간까지 참고하는 것이 편안한가?
- 현재 고려사항: 첫 MVP에서는 현재 기록에 집중하고, 과거 패턴은 사용자가 요청할 때만 불러오는 방식이 안전하다.

### Q-010 — 감정을 돌보는 제안의 깊이

- 상태: 프로토타입 후 검증
- 영향: AI 안전 기준, 콘텐츠 범위, 질문 길이
- 질문: 감정을 찾은 뒤 가장 받고 싶은 도움은 어떤 형태인가? 실제로 도움이 되었던 방식이 있다면 무엇인가?
- 현재 고려사항: 먼저 이해, 진정, 표현, 요청, 경계, 휴식 중 현재 목적을 물은 뒤 작은 행동 하나를 제안한다.

### Q-011 — 알림의 말투와 미루기

- 상태: 프로토타입 후 검증
- 영향: 지속 사용, 부담감, 취침 전 경험
- 질문: 밤 알림이 어떤 말투로 오면 부담 없이 열어볼 수 있을까? 놓친 날에는 다시 알려주기를 원하는가?
- 현재 고려사항: 연속 기록을 압박하지 않고 미루기와 오늘 건너뛰기를 허용한다.

## 구현 전 반드시 별도 설계할 안전 질문

### Q-012 — 위기 상황에서의 동작

- 상태: 답변 필요 — 외부 배포 차단
- 영향: 사용자의 즉각적 안전과 제품 책임
- 질문: 심각한 자해·자살·타해 위험이 드러날 때 앱이 감정 탐색을 멈추고 도움 연결을 안내하는 것에 동의하는가?
- 현재 고려사항: 외부 사용자에게 배포하기 전 지역에 맞는 안내와 전문가 검토가 필요하다.

### Q-016 — AI의 감정 후보 제시 방식

- 상태: 부분 답변됨 → DEC-037, 세부 수량은 프로토타입 후 검증
- 영향: 사용자의 자기결정권, 선택 피로와 탐색 속도
- 질문: 사용자가 스스로 감정·욕구를 먼저 살펴본 뒤 요청한 경우에만 AI가 근거와 함께 후보를 제안하는 방식이 편안한가? 한 번에 몇 개가 적절한가?
- 현재 고려사항: 사용자 자기 탐색을 AI보다 먼저 제공하는 순서는 DEC-037로 확정했다. 진단·단정 금지는 유지하고 보조 후보 3~5개 이내의 적절성은 도그푸딩으로 검증한다.

### Q-017 — 제품 로그 문서 형식 확정

- 상태: 답변 필요
- 영향: 장기 문서 관리와 추적성
- 질문: 현재 사용 중인 `requirements.md`, `product-log.md`, `decisions.md`, `open-questions.md` 분리 방식을 공식 운영 형식으로 확정할 것인가?
- 현재 고려사항: 로깅 자체는 REQ-011로 확정되었고, 문서 분리 형식은 DEC-008에 임시 운영으로 남긴다.

### Q-013 — AI 및 음성 처리 서비스

- 상태: 사용자 선택 대기 — 비교안 준비됨
- 영향: 구현 비용, 한국어 품질, 지연, 데이터 보존 정책
- 질문: 이미 선호하거나 사용할 수 있는 AI·음성 API가 있는가? 월간 사용 비용과 외부 전송에 대해 허용 가능한 기준은 무엇인가?
- 현재 비교안: `docs/references/ai-provider-comparison.md`에서 OpenAI, Gemini 유료 등급과 Anthropic의 공식 가격·학습·보존 기준을 비교했다. OpenAI Responses API의 `gpt-5.6-luna`를 비용 기준, `gpt-5.6-terra`를 품질 비교로 사용하는 안과 첫 개인 도그푸딩 프로젝트 전체의 월 $3 모델 비용 상한을 DEC-048에서 제안한다. 이는 1인당 정액 요금이 아니다.
- 모드별 비용안: `docs/references/ai-mode-cost-estimate.md`에서 개인 사용 기준 텍스트, 자유 음성, 턴형·Realtime 음성 핑퐁을 분리한다. DEC-049의 오프라인 산식 계산과 실제 호출 없는 합성 예산 차단은 승인되어 IMP-204A/B로 구현·검수 중이고, $1 목표 실제 공급자 합성 계측은 IMP-204C로 분리했다.
- 결정이 필요한 항목: 기본 안전 모니터링 로그의 최대 30일 보존 가능성 수용 여부, OpenAI 1차 평가 승인, 텍스트·음성별 월 비용 상한, IMP-204C의 $1 합성 계측 예산 목표와 마지막 요청의 소액 초과 가능성, 계정·키·서버 준비 범위. 승인 전 실제 공급자 연결과 외부 전송·과금 테스트는 금지한다.

### Q-014 — 이전 음성 인식이 불명확했던 표현

- 상태: 답변 필요
- 영향: 누락된 핵심 요구사항 가능성
- 질문: 앞선 발화의 “완전히 시드 완전 10초”는 어떤 의미였는가?
- 이유: 의미를 추측해 요구사항으로 확정하지 않기 위해 원문 상태로 남겨 두었다.

## 기술과 시각 방향에서 답할 질문

### Q-018 — MVP 기술 스택 확정

- 상태: 답변됨 → DEC-021, DEC-022, DEC-025
- 영향: 프로젝트 생성, 폴더 구조, 실행·테스트 명령과 이번 주 구현 속도
- 답변: `TypeScript + React Native + Expo + Expo Router`와 MSA가 아닌 모듈형 모놀리스 방향을 사용자가 수용했다.

### Q-019 — 시각 방향과 참고 화면

- 상태: 참고자료 수신, 프로토타입 후 검증
- 영향: 디자인 토큰, 컴포넌트 구조, 아이콘과 폰트 선택
- 질문: 참고 화면을 가져온 뒤 “부드러운 XP/90년대 개인용 다이어리 + 현대적 가독성” 방향과 더 절제된 현대 방향을 작은 목업으로 비교해도 되는가?
- 현재 고려사항: Windows Classic 계산기 이미지를 참고자료로 받았다. 각진 창, 입체 버튼, 회색 표면과 파란 제목 표시줄의 촉감을 차용하되 픽셀 복제는 하지 않는다. 관련 결정은 DEC-026, 해석은 `docs/references/windows-classic-ui.md`에 있다.

### Q-020 — 이번 주 MVP의 성공선

- 상태: 답변됨 → DEC-029, DEC-030
- 영향: 일주일 구현 범위와 검증 기기
- 답변: Android Expo Go에서 핵심 흐름을 먼저 만들고 안정되면 내부 APK로 전환한다. 컴퓨터 웹과 PWA는 같은 코드베이스의 다음 확장 단계로 둔다.

### Q-021 — Hallmark 프로젝트 스킬 설치 여부

- 상태: 기본안으로 진행 가능
- 영향: UI 작업 때 불러오는 외부 지침의 재현성과 유지보수
- 질문: UI 구현을 시작할 때 Hallmark를 `.codex/skills/hallmark/`에 프로젝트 범위로 설치할 것인가?
- 현재 고려사항: 지금은 공식 원본을 검토해 Inside Me용 참고 문서와 `AGENTS.md`에 적용했다. 설치하지 않아도 이 원칙은 지킬 수 있으며, 외부 코드와 다수의 상세 규칙을 프로젝트에 들이기 전에 실제 필요성을 확인하는 편이 안전하다.

### Q-022 — 클라이언트와 서버 폴더 구조 확정

- 상태: 답변됨 → DEC-027
- 영향: 프로젝트 스캐폴딩과 API 키 보안
- 답변: 별도 `frontend/`, `backend/` 프로젝트를 먼저 만들지 않고 하나의 Expo 프로젝트 안에서 `app/`, `src/features/`, `src/core/`, `src/infrastructure/`, `src/navigation/`, `src/platform/`, `src/testing/` 경계로 시작한다. `server/`는 외부 AI 비밀 키가 필요할 때만 추가하고 실제로 커질 때 별도 앱으로 추출한다.

### Q-023 — 첫 구현 세로 흐름 확정

- 상태: 답변됨 → DEC-028, DEC-037
- 영향: 이번 주 도그푸딩 시작 시점과 첫 화면 범위
- 답변: 먼저 `앱 열기 → 글로 하루 이야기 입력 → 사용자가 감정·욕구 어휘를 직접 탐색·선택 → 원할 때만 AI 또는 임시 탐색기의 보조 후보 확인 → 사용자가 최종 확정 → 폰에 저장 → 달력 날짜를 눌러 다시 보기`까지 작동시킨 뒤 같은 흐름에 음성을 붙인다. 문서에는 “세로 흐름”이라는 이름만 쓰지 않고 이 행동과 결과를 함께 기록한다.
- 변경 이력: 초기 DEC-028의 AI 후보 우선 흐름은 LOG-20260820-002와 DEC-037에서 사용자 자기 탐색 우선 흐름으로 대체됐다.

### Q-024 — 첫 Android 실행·설치 방식

- 상태: 답변됨 → DEC-029, DEC-030
- 영향: 이번 주 시작 속도, 개발 컴퓨터 의존과 폰 설치 경험
- 답변: Expo Go QR 코드로 Android 개발을 시작하고 핵심 흐름이 안정되면 내부 APK로 전환한다. PWA도 같은 코드베이스의 후속 범위에 포함한다.

### Q-025 — 첫 MVP 병렬 개발 운영 방식

- 상태: 답변됨 → DEC-034
- 영향: 작업 충돌, 통합 순서와 첫 Android 검증 시점
- 답변: 주 에이전트가 Expo 기반과 공통 계약을 먼저 고정한 뒤 `앱 핵심·로컬 저장`, `글 기록·감정 확인 UI`, `달력·날짜 상세 UI`의 세 작업을 병렬로 진행한다. 실제 AI 단계부터 프론트엔드와 서버 백엔드를 별도 역할로 분리한다.

### Q-026 — 지속 개발 워크플로 문서 구조

- 상태: 답변됨 → DEC-035
- 영향: 사용자가 `시작해`라고 요청한 뒤 다음 작업 선택, 병렬 분업, 검증, 인계와 중단 조건
- 답변: 상세 역할, 파일 소유권, 반복 실행 루프와 인계 형식은 `docs/development-workflow.md`에 기록하고, `AGENTS.md`에는 이 문서를 따르는 짧은 필수 실행 규칙을 둔다. 구현 단계와 완료 조건은 `docs/implementation-plan.md`를 원본으로 유지한다.

### Q-027 — 기록 내보내기 형식과 민감 파일 수명주기

- 상태: 프로토타입 후 검증
- 관련 제안: DEC-036, DEC-045
- 영향: 데이터 이동성, 개인정보, Android·PWA 플랫폼 경계와 실패 복구
- 질문: `schemaVersion`이 있는 UTF-8 JSON이 전체 기록을 읽고 옮기기에 충분한지, 공유 성공·취소·실패 뒤 앱 cache의 임시 파일이 모두 정리되는지 확인해야 한다.
- 현재 기본안: 합성 기록만 있는 테스트 프로필에서 검증하고, 파일명에는 사용자 이름·감정·일기 내용을 넣지 않는다. 실제 기록 내보내기는 사용자가 명시적으로 요청할 때만 수행한다.

### Q-028 — 사용자 우선 감정·욕구 탐색 화면의 정보 밀도

- 상태: 부분 답변됨
- 확정된 범위: 사용자 자기 탐색 우선 순서는 DEC-037
- 영향: 선택 피로, 모바일 가독성, 접근성과 첫 기록 완료 시간
- 질문: 검색 가능한 주제별 텍스트 목록, 전체 어휘를 보여 주는 `한눈에 보기`, 최근·자주 선택한 표현 중 어떤 조합이 실제로 가장 편안한가?
- 현재 권고안: 기본은 검색·주제별 필터·세로 스크롤·직접 추가가 가능한 텍스트 선택 UI로 구현한다. 제공 이미지는 초기 어휘와 보조 `한눈에 보기` 참고자료로 사용하며 작은 글자가 있는 고정 이미지를 기본 선택 화면으로 쓰지 않는다.

### Q-029 — 6시 이후 누적 Android 실기기 검증 방식

- 상태: 답변됨 → DEC-050
- 관련 결정: DEC-050
- 영향: IMP-106 완료 판정, 원격 개발 중 누적 변경의 회귀 검증, 검증 SHA 추적
- 질문: 6시 전 병합이 끝난 최신 `origin/main` SHA 하나를 기준으로 `docs/device-validation-checklist.md`의 필수 12단계를 순서대로 확인하는 방식을 저녁 실기기 검증 기준으로 확정할 것인가?
- 답변: 확정한다. 기능 번호마다 Expo를 다시 여는 대신 같은 최신 SHA에서 자동 검증 6개를 먼저 통과시키고, 실기기 전용 작성·선택·저장·복원·수정·내보내기·삭제 12단계와 접근성을 한 번의 누적 흐름으로 검증한다. 자동 검증이 끝난 비화면 순수 로직과 승인 전 실제 AI 계측은 제외한다.
