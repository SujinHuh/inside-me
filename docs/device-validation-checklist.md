# Inside Me Android 실기기 통합 검증 체크리스트

- 작성일: 2026-08-24
- 상태: 진행 중 — DV-01·02 사용자 선확인, 공식 기준선 재검증·DV-03 이후 대기
- 실행 시점: 사용자가 집에서 개발 Mac과 Android 폰을 함께 사용할 수 있다고 알린 뒤
- 관련 작업: IMP-106
- 관련 요구사항: REQ-026, REQ-028, REQ-029
- 관련 결정: DEC-029, DEC-047, DEC-050, DEC-055(DEC-064로 대체), DEC-060, DEC-061, DEC-064
- 관련 질문: Q-029, Q-034

## 목적

외부에서 폰으로 Codex에 접속하는 동안 완료된 작업을 기능 번호마다 따로 다시 실행하지 않는다. 저녁 검증 시작 직전에 최신 `origin/main` SHA 하나를 고정하고, 그 SHA까지 누적된 변경 중 Android 실기기에서만 확인할 수 있는 흐름을 한 번의 순서 있는 회귀 검증으로 확인한다.

순수 비용 계산기처럼 전체 자동 테스트와 독립 검수로 완료된 비화면 로직은 폰에서 같은 계산을 반복하지 않는다. SQLite 지속성, Expo Go 화면, 키보드, 공유 화면, 앱 재실행, 글꼴과 접근성처럼 정적 번들로 증명할 수 없는 항목만 이 문서에 모은다.

## 검증 기준선 고정

검증을 시작하기 전에 다음을 기록한다. 검증 중에는 다른 PR을 병합하지 않는다.

| 항목 | 기록 |
|---|---|
| 검증 날짜·시작 시각 | 2026-08-25 22:13 KST, 첫 Android 접속 시도 |
| Git root·`origin` 저장소 | `inside-me`, `https://github.com/SujinHuh/inside-me.git` |
| 현재 브랜치 | `main` |
| 로컬 `HEAD` 전체 SHA | `e63628bc0fb1d7c595010ea9a9bcf8af03c0d420` |
| `origin/main` 전체 SHA | `e63628bc0fb1d7c595010ea9a9bcf8af03c0d420` |
| Expo 앱 버전·SDK | `0.1.0`, Expo SDK 54 |
| Android 기기·OS | 사용자 확인 필요 |
| 실행 방식 | Expo Go |
| 개발 서버 연결 방식 | LAN, `exp://192.168.45.205:8081` |

6시 전에 새 PR이 더 병합되면 그 병합까지 끝난 최신 `origin/main`을 한 번만 받아 SHA를 다시 적는다. 로컬 `HEAD`와 `origin/main`의 전체 SHA가 정확히 같을 때만 앱을 실행한다. 검증 도중 브랜치·HEAD·코드가 바뀌면 기존 결과를 새 SHA의 성공 증거로 재사용하지 않는다.

## 준비

- [ ] 사용자가 개발 Mac과 Android 폰을 직접 사용할 수 있다고 확인했다.
- [ ] LAN 연결을 쓰면 Mac과 Android 폰이 같은 Wi-Fi에 연결됐다. LAN이 막히면 tunnel로 다시 실행하고 연결 방식을 기준선에 기록한다.
- [ ] Git root와 `origin` URL이 `SujinHuh/inside-me` 저장소를 가리키는지 확인했다.
- [ ] `git switch main`, `git fetch origin main`, `git pull --ff-only origin main` 순서로 동기화했다.
- [ ] 현재 브랜치가 `main`이고 로컬 `HEAD` 전체 SHA가 `origin/main` 전체 SHA와 같으며 작업 트리가 깨끗하다.
- [ ] `nvm use`와 `npm ci`가 통과했다.
- [ ] 실제 감정일기가 아닌 합성 이야기만 사용한다.

권장 합성 이야기: `프로젝트 발표 순서를 기다리며 긴장했지만 준비한 내용을 끝까지 설명했다.` 실제 감정이나 개인정보로 바꾸지 않는다.

## 같은 SHA의 자동 검증 근거

아래 명령은 사용자가 폰을 조작하기 전에 Codex가 고정된 SHA에서 실행하고 결과를 기록한다. 하나라도 실패하면 실기기 결과와 섞지 않고 먼저 수정·독립 재검수한다. 이전 PR의 성공 결과를 다른 SHA의 근거로 복사하지 않는다.

| 명령 | 결과·실행 시각 | 근거 PR·커밋 또는 실패 이유 |
|---|---|---|
| `npm run check:expo` |  |  |
| `npm test` |  |  |
| `npm run typecheck` |  |  |
| `npm run lint` |  |  |
| `npm run export:web` |  |  |
| `node scripts/expo-cli.cjs export --platform android` |  |  |

자동 검증 6개가 모두 통과한 뒤 `npm start`로 개발 서버를 열고, 표시된 QR을 Expo Go에서 연다. 이때 브랜치와 HEAD가 기준선에서 바뀌지 않았고 작업 트리가 계속 깨끗한지 한 번 더 확인한다.

## 필수 통합 흐름

각 단계는 앞 단계의 저장 결과를 이어서 사용한다. 실패하면 다음 단계로 넘기지 않고 단계 ID, 화면, 재현 순서와 기준 SHA를 기록한다.

| ID | 사용자 행동 | 기대 결과 | 상태 |
|---|---|---|---|
| DV-01 | Expo Go에서 최신 앱을 연다 | 홈 화면이 오류 없이 열리고 글 기록·달력 진입이 보인다 | 사용자 선확인 — 2026-08-25 22:25 KST 화면 확인. 자동 검증 6개 결과가 기록되지 않아 공식 통과 판정 대기 |
| DV-02 | `글로 기록하기`를 누르고 합성 이야기를 입력한다 | 키보드가 입력창과 다음 행동을 가리지 않고 입력 내용이 유지된다 | 사용자 선확인 — 2026-08-25 22:52 KST 잘림·겹침 없음. 자동 검증 6개 결과가 기록되지 않아 공식 통과 판정 대기 |
| DV-03 | 화면 구조를 미리 설명받지 않고 `이 화면에서 어떤 종류의 내용을 볼 수 있고, 원하는 단어를 어떻게 찾을 것 같은지 말해 주세요`에 답한다. `충족 감정 / 미충족 감정 / 욕구` 중 시작 방을 직접 고르고, 감정의 결 또는 욕구 8개 영역을 거쳐 실제 단어를 읽는다. 방을 바꿔 복수 선택·전체 검색·전체 목록·목록 밖 합성 표현 직접 추가를 사용한다 | 도움 없이 세 마음 방과 욕구가 8개 영역으로 이어진다는 구조를 설명하고 실제 단어 읽기를 시작한다. 충족 감정 70개·미충족 감정 91개·욕구 110개에 단계 탐색·전체 검색·전체 목록으로 모두 접근할 수 있고, 방·묶음·목록 이동 뒤에도 충족·미충족 감정과 욕구의 혼합 선택이 유지된다 | 대기 |
| DV-04 | 선택한 감정마다 서로 다른 강도를 지정하고 대표 감정을 직접 정한 뒤 저장한다 | 대표·추가 감정과 각각의 강도가 유지되고, 저장 완료 뒤 오늘 기록으로 이동하며 같은 날짜에 중복 기록이 생기지 않는다 | 대기 |
| DV-05 | 달력에서 오늘을 확인하고 날짜 상세를 연다 | 대표 감정 표정·정확한 이름·추가 감정 표시와 저장한 합성 이야기가 일치한다 | 대기 |
| DV-06 | Expo Go를 완전히 닫았다가 다시 열어 같은 날짜를 연다 | SQLite 기록이 복원되고 감정·욕구·강도·대표 감정이 유지된다 | 대기 |
| DV-07 | 기존 기록을 수정해 저장하고 다시 연다 | 같은 날짜 기록 한 개가 갱신되고 이전 값과 중복 행이 남지 않는다 | 대기 |
| DV-08 | 수정 화면에서 `AI와 더 살펴보기` 안내를 열고 먼저 취소한다 | 외부 미연결·전송 없음 안내가 보이고 취소 뒤 기존 글·선택이 유지된다 | 대기 |
| DV-09 | 다시 AI 도움을 요청하고 로컬 보조 후보 하나를 명시적으로 추가한다 | 요청 전에는 후보가 없고, 요청 뒤 후보가 자동 확정되지 않으며 누른 항목만 추가된다 | 대기 |
| DV-10 | 기록 내보내기를 눌러 Android 공유 화면을 열어 한 번 취소하고, 합성 기록으로 한 번 정상 공유한다 | 취소·성공 뒤 앱에 안전하게 돌아오고 원본 기록이 유지된다 | 대기 |
| DV-11 | 삭제 확인에서 먼저 취소한 뒤 다시 열어 삭제를 확정한다 | 첫 취소에서는 기록이 남고, 확정 뒤 달력·상세에서 기록이 사라진다 | 대기 |
| DV-12 | 앱을 완전히 닫았다가 다시 열어 삭제한 날짜를 확인한다 | 삭제한 기록이 복원되지 않고 빈 날짜 상태가 정상 표시된다 | 대기 |

### 검증 중 발견 사항

| ID | 발견 내용 | 분류·영향 | 상태 |
|---|---|---|---|
| UX-01 | Android에서 기존 한 장 마음 지도에 들어갔을 때 감정 선택 지점이 한눈에 들어오지 않고, 현재 미감이 감정 기록 화면으로서 딱딱하고 매력적이지 않다 | 데이터·기능 오류는 아니지만 DV-03의 탐색성과 감정 집중도를 해치는 사용성·시각 위계 문제 | A/B/C 비교와 320·360·412px 브라우저 보조 QA 뒤 DEC-064의 C안 `세 개의 마음 방`을 구현 기준으로 확정. IMP-107D 순수 계약·IMP-107E 앱 통합과 실제 Android 재검증 대기 |

`main@e63628b`의 **기존 앱** 320·360·412px 웹 모바일 QA에서는 글 입력→지도 진입, 세 영역 빠른 이동, 전체 검색과 선택 유지가 동작했고 확인한 선택 항목의 높이는 44 CSS px 이상이었다. 다만 화면 제목과 안내가 반복되고 소개 패널·검색·빠른 이동·모름 선택이 첫 화면 대부분을 차지해 실제 감정 단어가 아래로 밀렸다. 271개 어휘의 완전성은 유지됐지만 같은 무게의 칩을 긴 세로 목록으로 펼쳐 시작점과 현재 선택을 빠르게 파악하기 어려웠다. 이 결과는 **DEC-060 임시 목업의 배치 QA 결과가 아니다.** 웹 QA는 Android 키보드·손가락 조작감·TalkBack·실제 글꼴 렌더링을 대체하지 않는다.

### DV-03 첫 보기 증거

| 항목 | 기록 |
|---|---|
| 화면 버전·Git SHA |  |
| Android 기기·OS·글자 크기 |  |
| 질문 원문 | `이 화면에서 어떤 종류의 내용을 볼 수 있고, 원하는 단어를 어떻게 찾을 것 같은지 말해 주세요.` |
| 사용자 응답 요약 |  |
| 충족 감정 축 식별 | 예 / 아니요 |
| 미충족 감정 축 식별 | 예 / 아니요 |
| 욕구 축 식별 | 예 / 아니요 |
| 욕구 8개 영역 존재·접근 경로 이해 | 예 / 아니요 |
| 욕구 8개 영역 목록에 도움 없이 접근 | 예 / 아니요 |
| 검색 경로 이해 | 예 / 아니요 |
| 단계 탐색·전체 목록 경로 이해 | 예 / 아니요 |
| 도움 필요 여부 | 예 / 아니요 |
| 도움 없이 시작점 선택 | 예 / 아니요 |
| 실제 단어 읽기 시작 여부 | 예 / 아니요 |
| 첫 실패 지점 |  |

위 식별·이해·시작·읽기 항목이 하나라도 `아니요`이거나, `도움 필요 여부`가 `예`이거나, 진행자가 구조나 시작 위치를 설명했다면 DV-03을 통과시키지 않는다. 한 명의 성공은 해당 SHA의 개인 도그푸딩 증거이며 외부 사용자 전체의 성공률로 일반화하지 않는다.

### 화면 수치 측정 증거

| 항목 | 기록 |
|---|---|
| 대비 측정 도구 |  |
| 전경·배경 색상값과 계산 비율 |  |
| Android 조작 영역 측정 방법 | React Native Inspector / `hitSlop` 포함 코드 경계 / Accessibility Scanner / 기타 |
| 측정 대상과 최소 dp |  |
| 고정 UI 가림 여부와 캡처 |  |

## 기기 표시·접근성 확인

아래 항목은 자동 테스트가 대신할 수 없는 실제 Android 표시 확인이다. 설정 변경에 시간이 필요한 항목은 수행 여부와 이유를 남긴다.

- [ ] 감정 표정의 특수문자 눈·입이 네모 상자나 깨진 글자로 보이지 않는다.
- [ ] 작은 화면에서 감정·욕구 목록, 확인 버튼과 오류·안내 문구가 잘리거나 겹치지 않는다.
- [ ] Android 글자 크기를 키워도 핵심 문구와 다음 버튼에 접근할 수 있다.
- [ ] React Native Inspector 레이아웃 경계, `hitSlop`을 포함한 코드의 유효 터치 경계 또는 Accessibility Scanner 등 위에 기록한 방법으로 주요 감정·욕구 선택과 다음 행동의 실제 조작 영역이 48 × 48dp 이상임을 확인했다.
- [ ] TalkBack에서 세 영역 제목, 버튼·감정 이름·선택 상태·후보 도착 안내를 이해할 수 있다.
- [ ] 색상만으로 선택·오류·기록 유무를 구분하지 않는다.
- [ ] 위에 기록한 전경·배경 값과 도구로 일반 본문 대비 4.5:1, 큰 글자·핵심 그래픽 대비 3:1을 확인했고 포커스·선택 항목이 고정 UI에 가려지지 않는다.

## 결과 기록

| 결과 | 기록 |
|---|---|
| 같은 SHA 자동 검증 통과 수 |  / 6 |
| 필수 단계 통과 수 |  / 12 |
| 접근성 확인 수 |  / 7 |
| 발견한 오류 ID |  |
| 데이터 손실·중복 여부 | 없음 / 있음 |
| IMP-106 판정 | 대기 / 완료 / 오류 수정 후 재검증 |
| 검증 종료 시각 |  |

같은 SHA의 자동 검증 6개와 필수 실기기 12단계가 모두 통과하고 기준 SHA가 기록돼야 IMP-106을 `완료`로 바꾼다. 접근성 항목을 실행하지 못했으면 통과로 체크하지 않고 이유와 후속 작업을 남긴다. 오류 수정 커밋이 생기면 자동 검증을 새 SHA에서 다시 실행하고, 영향을 받는 실기기 단계와 필수 저장·재실행 흐름도 다시 확인한다.

공유 뒤 cache 임시 파일 정리는 화면만 보고 성공으로 판정하지 않는다. 자동 어댑터 테스트 결과와 함께 확인하고, 기기에서 잔여 파일을 확인할 수 없다면 Q-027의 미검증 항목으로 그대로 남긴다.

## 이 묶음에 포함하지 않는 항목

- IMP-204A/B의 비용 산식과 합성 예산 차단: 자동 테스트·독립 검수로 완료했으며 앱 화면에 연결되지 않았다.
- 실제 OpenAI 계정·API 키·외부 전송·과금 계측: IMP-204C 사용자 승인 전 금지한다.
- 실제 OpenAI 후보를 포함한 Android 통합 흐름: 이 묶음의 로컬 회귀로 대체하지 않고 IMP-205D에서 정상·취소·네트워크 실패·재시도·초안 보존을 별도로 확인한다.
- 내부 APK 설치·배포: Expo Go 전체 흐름이 안정된 뒤 DEC-029 단계에서 별도로 진행한다.
- 외부 사용자 공개: Q-012 위기 대응 흐름이 확정·검증되기 전에는 진행하지 않는다.
