# 감정·욕구 어휘 이미지 참고 노트

- 최초 수신일: 2026-08-18
- 재확인일: 2026-08-20 — 최초 수신 이미지와 동일한 파일을 다시 제공받음
- 정본 이미지: `2026-08-18-emotion-types.jpg`, `2026-08-18-feelings-needs-list.jpg`
- 이력 호환 경로: `2026-08-20-emotion-types.jpg`, `2026-08-20-feelings-needs-list.jpg` — 추가 전용 제품 로그의 과거 링크를 깨뜨리지 않기 위해 같은 원본을 보존하며 새 문서에서는 정본 경로만 사용
- 관련 요구사항: REQ-004, REQ-026, REQ-028
- 관련 결정: DEC-037, DEC-054(부분 대체), DEC-055
- 적용 상태: 전체 선택 표현 271개(감정 161개·욕구 110개) 구조화 및 한 장 마음 지도 구현, Android 실제 크기 검증 대기

## 이미지에서 확인한 구조

- 감정 어휘를 욕구가 충족됐을 때와 충족되지 않았을 때 연결되기 쉬운 표현으로 나눈다.
- 욕구 어휘를 자율성, 신체·생존, 관계, 놀이, 의미, 진실성, 평화, 자기구현처럼 큰 주제로 묶는다.
- 한 화면에 많은 어휘를 보여 주어 사용자가 AI의 해석 전에 자신의 표현을 직접 찾아볼 수 있게 한다.

## Inside Me에 적용할 원칙

- 이미지를 그대로 선택 화면으로 쓰지 않고 구조화된 어휘 데이터로 옮긴다.
- 모바일에서는 검색, 주제별 필터, 세로 스크롤과 선택 가능한 텍스트 항목을 제공한다.
- `욕구 충족과 연결된 느낌`, `욕구 미충족과 연결된 느낌`은 탐색을 돕는 필터일 뿐 감정의 좋고 나쁨이나 객관적 원인으로 저장하지 않는다.
- 목록에 없는 표현을 직접 추가할 수 있고 `아직 모르겠어요`, `AI 도움 받기`로 넘어갈 수 있게 한다.
- 자유 글, 자유 음성, AI 턴 대화가 서로 다른 입력을 끝낸 뒤 같은 자기 탐색 화면과 데이터 계약을 사용한다.
- 사용자가 먼저 고른 항목, AI가 나중에 제안한 항목과 최종 확정 항목을 데이터에서 구분한다.

## 확정된 UI 적용

1. 기본 진입은 충족 감정, 미충족 감정, 욕구 8개 영역을 같은 세로 화면에 펼친 `한 장 마음 지도`다.
2. 어느 분류를 먼저 선택하도록 강요하지 않으며 세 영역을 오가며 복수 선택할 수 있다.
3. 전체 검색은 감정과 욕구를 동시에 좁히고, 목록 밖 표현은 감정·욕구를 구분해 직접 추가한다.
4. 색은 탐색 영역을 구분하는 보조 수단으로만 쓰며 정확한 이름과 선택 표시를 함께 제공한다.
5. 기존 Windows Classic의 파란 제목 표시줄과 창 경계는 앱 정체성으로 유지하되, 지도 내부는 부드러운 컬러 면과 둥근 선택 항목을 사용한다.
6. 마지막에만 `AI와 더 살펴보기`를 선택적으로 제공하고 AI가 사용자의 선택을 자동 확정하지 않는다.

고정 이미지는 `한눈에 보기` 보조 자료가 될 수 있지만 기본 선택 UI로 사용하지 않는다. 이미지 속 작은 글자는 접근성이 낮고 선택 상태, 검색, 스크린 리더, 어휘 수정과 직접 추가를 지원하기 어렵기 때문이다.

## 검토가 필요한 점

- 일부 표현은 감정이라기보다 생각, 평가나 상태로 해석될 수 있으므로 정답 분류로 취급하지 않는다.
- 같은 감정도 여러 욕구와 연결될 수 있으므로 감정과 욕구를 일대일로 자동 연결하지 않는다.
- 원본 이미지의 반복 표현은 선택 항목 하나로 정규화한다. 판독 가능한 괄호 속 보조 표현은 검색 별칭으로 보존하되, 글자 단위 판독이 불명확한 설명은 선택 이름이나 별칭으로 확대 해석하지 않는다.
- 긴 목록이 선택 피로를 만들지 않는지 Android 실제 크기로 검증한다.
