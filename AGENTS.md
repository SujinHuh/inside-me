# Inside Me — AI 개발 지침

이 파일은 `inside-me/` 전체에 적용되는 저장소 지침이다. 새 Codex 실행을 `inside-me/` 또는 그 하위 디렉터리에서 시작해야 이 파일이 프로젝트 지침 탐색 경로에 포함된다. 기존 세션에서 도구의 `workdir`만 바꿔도 지침이 자동 재로딩되지는 않는다. 하위 폴더에 더 구체적인 `AGENTS.md` 또는 `AGENTS.override.md`가 생기면 가까운 지침을 우선한다.

이 파일에는 반복해서 지켜야 하는 실행 규칙만 둔다. 제품 요구사항 전문, 대화 이력과 결정의 상세 내용은 `docs/`를 원본으로 삼아 중복과 불일치를 피한다.

## 제품의 목적

Inside Me는 사용자가 막연한 기분을 구체적인 감정 언어로 알아차리고, 그 감정과 연결된 상황과 욕구를 스스로 탐색하도록 돕는 개인용 감정 기록 앱이다.

AI는 사용자의 마음을 판정하거나 진단하는 역할이 아니다. 사용자가 자신의 경험을 더 잘 표현하고 직접 선택하도록 돕는 질문자이자 기록 도우미다.

## 문서별 원본 역할

- `docs/requirements.md`: 현재 유효한 제품 요구사항과 MVP 범위
- `docs/decisions.md`: 결정 상태, 대안, 트레이드오프와 재검토 조건
- `docs/product-log.md`: 사용자 제품 발화와 당시 해석을 보존하는 추가 전용 로그
- `docs/open-questions.md`: 답이 필요한 질문과 프로토타입 후 검증할 질문
- `docs/references/`: 사용자가 제공했거나 제품 근거로 채택한 참고자료
- `docs/implementation-plan.md`: 사용자의 행동으로 풀어 쓴 구현 순서, 단계별 완료 기준과 다음 작업 인계
- `docs/development-workflow.md`: 역할별 책임, 병렬 작업, 통합, 검증, 지속 구현 루프와 인계 형식

문서가 충돌하면 최신 사용자 발화를 먼저 `product-log.md`에 보존하고, 확정 여부를 판단한 뒤 요구사항과 결정 문서를 갱신한다. AI의 추정이나 제안을 사용자 결정으로 취급하지 않는다.

## 작업 시작 순서

1. `docs/requirements.md`에서 현재 MVP 범위와 관련 `REQ-*`를 확인한다.
2. 관련 `DEC-*`, `Q-*`, 최근 `LOG-*`를 읽어 결정 상태와 원래 맥락을 확인한다.
3. 이 프로젝트 범위의 작업 트리 상태와 기존 파일을 확인하고 사용자의 변경사항을 보존한다.
4. 결정되지 않은 제품·기술 선택이 필요한지 확인한다. 되돌리기 쉬운 기본안은 `제안됨`으로 기록하고, 범위를 크게 바꾸는 선택은 사용자 결정 없이 확정하지 않는다.
5. 매일 실제로 사용할 수 있는 가장 작은 세로 흐름을 구현하고 검증한다.

## 지속 구현 지침

사용자가 `시작해`, `개발 시작해` 또는 현재 구현을 계속하라고 명시하면 `docs/development-workflow.md`의 지속 구현 루프를 따른다.

1. `docs/implementation-plan.md`의 실행 상태 원장에서 현재 작업 ID와 선행 조건을 선택한다.
2. 공통 계약과 선행 작업이 없으면 주 에이전트가 먼저 고정한다.
3. 안전하게 분리되고 독립 검증 가능한 작업만 소유 경로와 수정 금지 파일을 지정해 병렬 실행한다.
4. 주 에이전트가 결과를 통합하고 정상·실패·데이터 보존 흐름을 검증한다.
5. 구현 단위가 자동 검증을 통과하면 구현에 참여하지 않은 서브에이전트가 기준 SHA와 diff를 읽기 전용으로 독립 검수한다. 확인된 오류가 하나라도 남으면 다음 구현으로 넘어가지 않고, 주 에이전트가 수정·재검증·재검수한다.
6. 서브에이전트 검수에서 확인된 오류가 없을 때만 현재 구현을 `완료`로 바꾸고, 구현 계획, 관련 요구사항·결정·질문과 제품 로그를 갱신한다.
7. 사용자가 이번 요청에서 `계속` 또는 `끝까지`를 명시한 경우에만 현재 번호 단계 안에서 다음 작업을 반복한다.

기본 실행 단위는 현재 단계의 가장 작은 세로 조각 하나 또는 PR 하나다. 조각 내부의 하위 작업은 구현·통합·자동 검증·서브에이전트 독립 검수·인계까지 끝내고, 오류 없음이 확인된 조각을 끝내면 해당 턴을 종료해 사용자 체크포인트를 만든다. 서브에이전트를 사용할 수 없는 환경에서는 주 에이전트의 자체 리뷰로 대체하지 말고 `독립 검수 대기`로 멈춘다. 다음 번호 단계, 제품 범위를 바꾸는 미결정 사항, 유료·외부 서비스, 비밀정보나 민감정보 전송, 사용자의 실제 기기 조작, 이번 요청에 명시되지 않은 푸시·PR, 병합·배포 또는 같은 원인의 검증 실패 3회가 필요하면 현재 결과와 재개 지점을 남기고 사용자에게 요청한다. `시작해`만으로 원격 변경 권한을 추정하지 않는다.

여러 에이전트가 같은 작업 디렉터리를 사용할 때 서브에이전트는 지정된 소유 경로만 수정하고 Git 명령과 공통 문서 ID 갱신을 하지 않는다. 주 에이전트가 공통 계약, 통합, 검증, 문서와 Git 상태를 최종 관리한다.

## 작업 원칙

- 이 프로젝트는 솔로 개발자의 실제 도그푸딩을 최우선으로 한다. 추상적인 확장성보다 매일 사용할 수 있는 가장 작은 완성 흐름을 먼저 만든다.
- 결정되지 않은 제품·기술 선택을 임의로 확정하지 않는다. 가정을 사용했다면 문서에 명시한다.
- 기술 용어는 이름만 쓰지 않는다. 사용자가 어느 화면에서 무엇을 하고 앱이 어떤 결과를 보여 주거나 저장하는지, 포함·제외 범위와 완료 조건을 함께 풀어서 기록한다.
- 새로운 외부 서비스, 유료 의존성, 사용자 계정 또는 복잡한 인프라를 추가하기 전에 필요성과 대안을 설명한다.
- 관련 기능을 변경하면 요구사항과 제품 결정 문서도 함께 갱신한다.
- 사용자가 만든 기존 변경사항과 기록을 보존한다.
- 요청과 무관한 리팩터링, 파일 이동, 대규모 포맷 변경을 함께 수행하지 않는다.
- 동작 또는 데이터 형식을 바꾸면 호환성, 기존 로컬 기록과 마이그레이션 영향을 확인한다.
- 공유 Git 루트의 다른 프로젝트를 건드리지 않는다. 파일 수정, 포맷, 스테이징과 커밋은 명시적 요청이 없는 한 `inside-me/` 안으로 제한한다.
- 메인, 달력, 감정 어휘, 자유 음성 기록, 자유 글 기록, AI 턴 대화, 알림, 저장과 외부 연동을 기능 경계별 모듈로 나눈다. 화면과 라우트는 기능을 조합하는 얇은 진입점으로 유지한다.
- 화면마다 서버나 외부 API를 만들지 않는다. 녹음, 전사, 음성 합성, AI, 알림과 저장은 교체 가능한 포트·어댑터 경계 뒤에 두고, 음성과 글 입력은 감정 탐색·확인·저장 핵심 로직을 공유한다.
- UI를 설계, 구현 또는 리뷰하기 전에 `docs/references/hallmark.md`와 `docs/references/windows-classic-ui.md`를 읽고 현재 화면에 관련된 anti-AI-slop 점검과 사용자 레퍼런스의 디자인 DNA를 적용한다. 감정·욕구 자기 탐색 화면은 `docs/references/emotion-needs-vocabulary-images.md`도 읽는다. 사용자 레퍼런스, 감정적 안전, 접근성과 확정된 Inside Me 디자인 시스템이 외부 디자인 스킬의 기본값보다 우선한다.
- 상세 기능과 이력은 이 파일에 복사하지 말고 원본 문서를 연결한다. 아래의 안전·개인정보 가드레일은 모든 작업에서 빠지면 안 되므로 의도적으로 요약해 반복한다.

## 제품 대화 로깅 프로토콜

제품 아이디어, 요구사항, 디자인, 기술 선택, 우선순위 또는 트레이드오프가 등장한 모든 대화 턴에 적용한다.

1. 응답을 끝내기 전에 `docs/product-log.md`의 마지막 ID를 확인하고 다음 순번의 `LOG-YYYYMMDD-NNN` 항목을 추가한다. 같은 날짜의 번호는 중복하거나 재사용하지 않는다.
2. 사용자의 제품 지시 발화는 음성 인식 오류가 있어도 의미를 임의로 고치지 말고 원문을 보존한다. 다만 실제 일기·전사·토큰·개인식별정보는 `[민감 내용 생략]`으로 바꾸며, 별도의 “해석” 항목에서 제품 지시만 정리한다.
3. AI가 제시한 제안, 대안, 가정과 질문도 같은 로그 항목에 요약해 남긴다.
4. 결정이 생기거나 후보가 제안되면 `docs/decisions.md`에 `DEC-NNN` 항목을 추가하거나 상태를 갱신한다.
5. 결정 항목에는 최소한 맥락, 선택지, 현재 상태, 선택 또는 제안 이유, 장점, 감수하는 비용·위험, 재검토 조건을 기록한다.
6. 사용자가 확정하지 않은 AI 제안은 반드시 `제안됨`으로 표시한다. 침묵이나 다음 대화로의 진행을 동의로 간주하지 않는다.
7. 현재 유효한 요구가 달라지면 `docs/requirements.md`를 함께 갱신하고 관련 `REQ-NNN`, `LOG-*`, `DEC-*`를 서로 연결한다.
8. 과거 로그는 덮어쓰거나 삭제하지 않는다. 오해나 오류는 새 로그로 정정하고, 결정은 `대체됨` 상태와 후속 결정 링크로 이력을 남긴다.
9. 사용자가 제공한 제품 참고 자료가 임시 경로에 있으면 영구 보존이 허용된 범위에서 `docs/references/`에 복사하고 로그에서 연결한다.
10. 최종 응답에서 이번 턴에 추가·변경한 로그, 요구사항, 결정 ID를 짧게 알린다.
11. 새 미결정 사항은 `docs/open-questions.md`에 `Q-NNN`으로 추가하고, 답을 얻으면 상태와 연결된 결정 ID를 갱신한다.

여러 에이전트가 함께 작업할 때 제품 로그와 결정 문서의 최종 갱신은 기본적으로 주 에이전트가 담당한다. 서브에이전트는 명시적으로 문서 편집을 위임받지 않았다면 검수 결과만 주 에이전트에게 전달해 ID 충돌을 피한다.

로깅 대상은 제품 개발 대화다. 실제 감정일기 내용, 인증정보, API 키와 불필요한 개인정보는 사용자가 명시적으로 요청하지 않는 한 개발 문서에 기록하지 않는다. 사용자가 특정 내용을 기록하지 말라고 하면 그 지시를 우선한다.

## 감정 탐색 AI 원칙

- 기본 사용자 언어와 UI 문구는 한국어다.
- 한 번에 질문 하나만 하고, 사용자가 말한 내용을 짧게 반영한 뒤 다음 질문을 한다.
- “당신의 진짜 감정은 X다”처럼 단정하지 않는다.
- 감정 후보를 제시하는 경우 그 이유를 설명하고 최종 선택권은 항상 사용자에게 둔다. 후보의 개수와 제시 방식은 확정된 결정과 도그푸딩 결과를 따른다.
- 자유 글·자유 음성·AI 턴 대화가 끝난 뒤에는 AI 후보보다 사용자의 감정·욕구 자기 탐색을 먼저 제공하고, 사용자가 요청할 때만 AI가 추가 후보를 제안한다.
- 사용자의 표현을 교정하거나 훈계하지 않는다. 관찰, 해석, 감정, 욕구를 부드럽게 구분하도록 돕는다.
- 비슷한 감정의 차이를 일상적인 한국어로 짧게 설명한다.
- 불쾌한 감정을 제거해야 할 문제로 취급하지 않는다. 먼저 이해하고, 사용자가 원할 때만 진정·표현·행동 방법을 제안한다.
- 감정·욕구 목록은 탐색을 위한 비포괄적 어휘 사전으로 사용하며 정답표처럼 사용하지 않는다.
- 정신질환, 성격, 타인의 의도, 관계의 결론을 진단하거나 추정하지 않는다.
- 자해·자살·타해 또는 즉각적인 위험 신호가 있으면 일반 감정 코칭을 계속하지 말고 별도의 안전 대응 흐름을 따른다.
- `docs/open-questions.md`의 위기 대응 흐름이 확정되고 검증되기 전에는 외부 사용자에게 배포하지 않는다. 개인 로컬 도그푸딩만 허용한다.

## 개인정보 원칙

- 감정 기록과 음성은 민감정보로 취급한다.
- 원본 음성은 전사와 분석이 성공하면 삭제하며 영구 저장하지 않는다.
- 저장, 전송, 외부 AI 처리 여부를 사용자가 이해할 수 있게 표시한다.
- 삭제와 내보내기가 가능한 데이터 구조를 유지한다.
- 로그, 분석 도구, 오류 메시지에 일기 원문이나 음성 전사를 노출하지 않는다.
- 실제 감정일기 원문을 fixture, snapshot, 예제 데이터 또는 프롬프트 테스트에 복사하지 않는다. 합성 데이터만 사용한다.

## 기술 스택과 실행 명령

MVP 스택은 TypeScript, React Native, Expo와 Expo Router이고 서버 배포 구조는 MSA가 아닌 모듈형 모놀리스로 확정되었다. 첫 구현은 글 기록에서 감정 확인·로컬 저장·달력 다시 보기까지 작동시키는 흐름이며, Android Expo Go QR 실행으로 검증한 뒤 내부 APK와 PWA로 확장한다. Q-022와 DEC-027에 따라 하나의 Expo 프로젝트 안에서 `app/`, `src/features/`, `src/core/`, `src/infrastructure/`, `src/navigation/`, `src/platform/`, `src/testing/`을 나누는 구조가 확정됐다. `app/api/`와 `server/`는 외부 AI 비밀 키가 필요한 2단계 전에는 생성하지 않는다.

실제 명령은 `package.json`을 원본으로 삼는다.

- Node 버전 맞추기: `nvm use` (`.nvmrc`: 24.15.0 LTS)
- 설치: `npm ci`
- 로컬 개발 실행·Expo Go QR: `npm start`
- Expo SDK 호환성: `npm run check:expo`
- 단위·계약 테스트: `npm test`
- 린트: `npm run lint`
- 타입 검사: `npm run typecheck`
- 정적 웹 번들: `npm run export:web`

화면 통합 테스트는 관련 화면이 생기는 IMP-003B 이후 추가한다. Android 프로덕션 빌드는 내부 APK 단계에서 EAS 사용 승인 후 정의한다. 존재하지 않는 스크립트를 추측하지 말고 저장소 설정 파일에서 실제 명령을 확인한다.

## 의존성, 외부 서비스와 비밀정보

- 새 프로덕션 의존성이나 외부 서비스는 해결하는 문제, 대안, 비용, 개인정보 영향과 제거 가능성을 먼저 설명한다.
- 유료 서비스, 민감정보 외부 전송, 사용자 계정, 되돌리기 어려운 인프라는 사용자의 명시적 승인 없이 추가하지 않는다.
- API 키, 토큰과 인증정보를 코드, Git, 클라이언트 번들, 로그 또는 문서에 넣지 않는다.
- 예시 환경 파일에는 값이 없는 변수 이름과 설명만 둔다.
- 감정 기록 동기화 서버와 외부 AI 요청을 위한 중계 백엔드를 별개의 결정으로 취급한다.
- 외부 AI·전사·음성 합성 서비스를 채택하기 전에 한국어 품질, 지연, 비용, 데이터 보존·학습 사용 정책과 삭제 가능성을 확인한다.
- 클라이언트에서 공급자 API를 직접 호출해 비밀 키를 노출하지 않는다.

## 구현과 검증

- 기능을 세로로 완성한다: 입력 → 탐색 → 확인 → 저장 → 다시 보기 흐름이 실제로 동작해야 한다.
- 핵심 감정 기록 데이터의 손실과 중복 저장을 우선 테스트한다.
- 하루 한 개 기록 규칙, 기존 기록 수정, 앱 재시작 후 복원과 완전 삭제를 테스트한다.
- 삭제·손상·마이그레이션 테스트는 격리된 namespace 또는 임시 DB의 합성 데이터에서만 실행하고 실제 앱 저장소 전체를 초기화하지 않는다.
- 음성 전사 성공 후 원본 오디오 삭제와 실패·재시도 시 임시 파일 정리를 테스트한다.
- AI 출력은 스키마 검증과 실패 시 복구 경로를 갖춘다.
- AI가 감정 후보를 단정으로 표현하지 않는지, 잘못된 형식·네트워크 실패·중단된 대화를 복구하는지 확인한다.
- 접근성, 모바일 화면, 키보드 입력, 음성 사용 후 전사 수정 흐름을 확인한다.
- 안드로이드폰을 주 도그푸딩 환경으로 검증하고 컴퓨터 사용 경로도 최소 한 번 확인한다.
- Android 실기기 결과는 사용자 성공 응답과 검증한 커밋 SHA 또는 앱 버전을 기록하기 전까지 `완료`로 표시하지 않는다. 에뮬레이터·웹·빌드 성공으로 대체하지 않는다.
- 코드 변경 후 저장소에 정의된 관련 테스트, 린트, 타입 검사와 빌드를 실행한다. 실행하지 못한 항목은 이유를 남긴다.
- 구현 완료를 보고할 때 실제로 검증한 항목과 아직 검증하지 못한 항목을 구분한다.

## 코드 리뷰 규칙

다음 문제를 발견하면 완료로 처리하지 않고 수정하거나 명시적으로 보고한다.

- 일기 원문, 음성, 전사 또는 감정 결과가 일반 로그·분석·오류 수집에 노출됨
- 원본 음성이 성공 처리 후에도 남아 있음
- API 키 또는 비밀정보가 클라이언트나 저장소에 포함됨
- AI가 감정, 정신질환, 성격, 타인의 의도를 사실처럼 단정함
- 사용자의 선택 없이 AI 추론이 확정 감정이나 확정 요구사항으로 저장됨
- 네트워크·전사·저장 실패가 기록 손실 또는 중복을 조용히 일으킴
- 접근성 레이블, 키보드 경로, 음소거 또는 텍스트 대체 수단이 없음

## PR 작성 규칙

- 모든 PR은 `.github/pull_request_template.md` 형식을 사용하고 `docs/pull-request-guide.md`의 작성 원칙을 따른다.
- 실제 변경에 해당하는 유형만 선택하고, 변경 내용과 이유를 현재 작업에 맞게 다시 작성한다.
- 실행한 검증만 체크하며 실행하지 못한 검증은 이유를 남긴다.
- 제품 동작이나 결정에 영향을 주면 관련 `REQ-*`, `DEC-*`, `LOG-*`를 연결한다.
- 민감정보 외부 전송, 비밀정보, 기존 로컬 기록의 호환성과 마이그레이션 영향을 확인해 PR에 명시한다.
- 완료 기준을 충족하지 않았거나 설계 검토가 목적이면 Draft PR로 표시한다.

## 완료 기준

작업은 다음 조건을 충족할 때 완료로 보고한다.

- 요청한 동작이 현재 요구사항과 결정에 맞게 구현되었다.
- 관련 정상 흐름, 실패 흐름과 데이터 보존·삭제 동작을 검증했다.
- 새 가정과 트레이드오프가 로그와 결정 문서에 남았다.
- 변경된 동작에 맞춰 요구사항과 미결정 질문 상태를 갱신했다.
- 비밀정보와 실제 감정일기 데이터가 코드, 테스트, 로그에 포함되지 않았다.
- 최종 응답에 변경 파일, 검증 결과, 남은 위험과 관련 `REQ-*`·`DEC-*`·`LOG-*`를 요약했다.
